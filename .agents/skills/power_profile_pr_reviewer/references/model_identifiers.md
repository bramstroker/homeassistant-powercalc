# Canonical Model Identifier Reference

Use this reference when a PR adds or renames a power-profile model directory. Its patterns are review clues, not validation regexes: manufacturers introduce new families, and an identifier's shape alone never proves that it is canonical or unique.

## Decision rule

The directory name is the stable, specific manufacturer model code for the measured hardware. Discovery values that other integrations report for the same hardware belong in `aliases`.

Prefer evidence in this order:

1. Manufacturer technical data, rating label, manual, support page, or official device API.
2. Home Assistant Device information and integration diagnostics. Check both `model` and `model_id`; record which integration supplied the value.
3. An established protocol catalog such as Zigbee2MQTT only as corroboration, especially when it maps a device signature to a manufacturer code.
4. Retail listings, forum posts, and search snippets only as leads to verify elsewhere.

The PR generator cannot determine which reported value is canonical in every integration. A value shown as “Model ID” in a generated PR may still be an integration description or bridge-generated resource name.

## Universal exclusions

Do not use these as a directory name or alias:

- Home Assistant entity names, user-assigned device names, or room names;
- serial numbers, MAC addresses, network addresses, and bridge resource names such as `Light0x...`;
- a marketing family name that covers multiple hardware variants;
- a generic white-label or protocol identifier shared by materially different products, unless the manufacturer-specific guidance explicitly documents it as an integration-reported discovery alias that PowerCalc safely presents as multiple candidates;
- a numeric Matter product ID unless there is evidence that it uniquely identifies the physical model.

If no unique identifier can be established, request manual verification. If the device exposes only a generic identifier, it may be suitable only as a custom profile.

## Manufacturer guidance

| Manufacturer | Common canonical identifiers in this library | Where to confirm | Common alias or rejection cases |
|---|---|---|---|
| 3A Smart Home / Nue | Prefer a verified product-specific code printed on the complete device or packaging, such as `WL-SD001-9W`; retain an exact `LXN...` or `LXT...` Zigbee value while no more specific code is verified | Product or packaging label and manufacturer technical data; then HA Device Info and the raw Zigbee signature | `LXN...` and `LXT...` values can identify the embedded controller and may be shared by different complete products. Follow the detailed 3A Smart Home guidance below. |
| Amazon | Printed hardware model codes such as `B7W64E`, `C2H4R9`, and `L9D29R` | Product label, regulatory filings and official device documentation; then HA Device Info and Alexa integration diagnostics | ASINs are retail catalog identifiers, while Alexa device types and family-prefixed values are discovery aliases. Existing or confirmed Amazon serial/DSN discovery aliases are an intentional exception. Follow the detailed Amazon guidance below. |
| Antela | Exact printed product code; retain a product-specific HA model such as `ANTELA-9W-RGB+CCT` when retail package codes conflict | Product label, the exact original retail listing, official Antela product data, exact HA Tuya Device Info, and the original profile PR | Do not promote a current package SKU when the same ASIN has exposed different model numbers over time. Preserve an exact opaque Tuya product ID only when mapped to the measured product. |
| Apple | Printed Apple hardware model numbers such as `A2374` and `A2825` | Apple regulatory product tables and information guides; then HA Apple TV Device Info and pyatv model mappings | Regional or color-specific `M...` order/SKU codes are not the canonical hardware model. Preserve exact pyatv names as discovery aliases. Follow the detailed Apple guidance below. |
| IKEA | Product codes such as `E2499`, `E2206`, `LED2405G8`, `L2206`, `T1828`, and driver codes such as `ICPSHC24-30EU-IL-1` | IKEA product page technical information under **Model identifier**, rating label/manual, then HA/ZHA or Zigbee diagnostics | Hue may report a descriptive value such as `VARMBLIXT colour and white`; keep that as an alias when needed. Reject Hue resource names such as `Light0x...`. Matter numeric IDs are often not unique. |
| Signify / Philips Hue | Zigbee model codes such as `LCA...`, `LCT...`, `LWA...`, `LTW...`, and stable luminaire/product codes such as `92900...` when that is the device model | Hue API v2 device `product_data.model_id`, HA Hue Device information, product label, then the Zigbee2MQTT device definition | Do not confuse a retail EAN, user-assigned Hue name, or bridge resource name with the device model. Preserve exact integration-normalized article codes as aliases, including a shared alias when multiple raw Hue models are intentionally grouped. Follow the detailed Signify guidance below. |
| SmartHomeShop | Exact ESPHome project-model segments such as `ultimatesensor_mini` and `ultimatesensor_mini_v2` | Official firmware `esphome.project.name`, then exact HA ESPHome Device Info and official hardware documentation | Preserve underscores because they are part of the discovery identifier. Distinguish hardware generations in the display name and do not assume V1 and V2 share power behavior. Follow the concise SmartHomeShop guidance below. |
| SNOOZ | Printed underside model codes such as `SNOOZ-US-2` | Product label and original profile evidence, then official product documentation | The HA SNOOZ integration currently omits manufacturer and model, so aliases cannot enable automatic discovery. Follow the concise SNOOZ guidance below. |
| Shelly | Hardware codes such as `SHPLG-S`, `SNSW-...`, `S3SW-...`, `S4SW-...`, and `SPEM-...` | Gen2+ `Shelly.GetDeviceInfo` response field `model`; Gen1 `/shelly` device info; product label and official documentation | Names such as `Shelly Plus Plug S` are useful display names but are not preferable to the hardware code. Never use the device ID containing its MAC suffix. |
| TP-Link Kasa / Tapo | Exact HA model such as `HS110`, `KP115`, `P110`, `P110M`, `L530`, or `L900` | HA Device Info and Kasa/Tapo Device Info, then product label/body and official support page | Prefer the integration-reported family model over a longer retail variant when HA intentionally strips the fitting, length, or region suffix. Preserve verified region strings and Matter product IDs as aliases. Follow the concise TP-Link guidance below. |
| Treatlife | Printed product codes such as `SL10` | Official Treatlife manual/support page and product label, then exact HA Tuya Device Info and the original profile PR | Tuya can report an opaque product identifier instead of the printed model. Preserve an exact verified value as a model alias together with `Tuya` as a manufacturer alias. Follow the concise Treatlife guidance below. |
| Tripp Lite | Exact generic NUT model strings such as `Tripp Lite UPS` when the device exposes no specific hardware model | Exact HA NUT Device Info and the raw NUT `device.mfr`/`device.model` values | Keep a generic profile generic when its calculation uses the exposed nominal VA and load. Use a descriptive name that does not repeat the discovery model. Follow the concise Tripp Lite guidance below. |
| Trust / KlikAanKlikUit | Official `ZLED-...` product codes such as `ZLED-G2705` | Official Trust datasheet and product label, then exact HA Zigbee Device Info and the original profile PR | Trust products can be co-branded as KlikAanKlikUit. Add that name as a manufacturer alias only when HA reports it, not merely from retail branding. Follow the concise Trust guidance below. |
| Velux | Exact HA gateway model `KLF 200` | Home Assistant's Velux gateway Device Info implementation and official VELUX product documentation | Preserve the space in `KLF 200` and use the official interface description for the profile name. Follow the concise Velux guidance below. |
| VeSync / Levoit | Exact HA VeSync models such as `Core200S`, `Core300S`, and `Core400S` | Exact HA VeSync Device Info and the original profile PR, then the official Levoit product page | Preserve the integration-facing model without spaces. Treat spaced models and retail `-P` suffixes as product-page variants unless HA reports them. Follow the concise VeSync guidance below. |
| Wyze | Exact HA Wyze models such as `WLPA19C` and `WLPP1CFH` | Exact HA Device Info and the original profile PR, then the official Wyze product page | Preserve `WyzeLabs` as a manufacturer alias when HA reports it. Strip the Wyze brand and model from the concise product name. Follow the concise Wyze guidance below. |
| Xiaomi ecosystem | Printed Xiaomi hardware codes such as `MJDPL01YL` and `BPLDS02DM`; retain an exact integration identifier such as `lumi.gateway.mgl03` when the measured hardware variant is unclear | Official Xiaomi product specifications, compliance declarations, or product label; then exact HA Device Info and the original profile PR | `yeelink.*`, `lumi.*`, and `dmaker.*` identifiers can represent integration or protocol families. Add them as aliases only when uniquely mapped to the measured hardware. Follow the concise Xiaomi guidance below. |
| Yeelight | Printed `YL...` or `MJ...` hardware code | Official Yeelight/Xiaomi product documentation and label; then exact HA Device Info, Home Assistant's Yeelight device table, and the original profile PR | Preserve unique integration identifiers such as `mono1`, `color2`, `strip6`, `ceiling1`, `ceiling10`, or `ceild` as aliases. Do not add shared family identifiers such as `color1`, `color4`, or `bslamp1`. Follow the concise Yeelight guidance below. |
| Ynoa | Printed `LA-...` article number | Official Ynoa product listing or manual, then Zigbee2MQTT's device definition and exact HA Device Info | Preserve a unique raw `ZBT-...` Zigbee model as a discovery alias while keeping the article number canonical. Follow the concise Ynoa guidance below. |
| YouLess | Exact device model such as `LS120` | YouLess documentation and Home Assistant's YouLess Device Info implementation | Do not alias a different hardware generation such as `LS110`; firmware variants do not replace the hardware model. Follow the concise YouLess guidance below. |
| Zemismart | Printed model or article number when one is documented; otherwise retain the established released identifier | Official Zemismart product page and label, then exact integration Device Info, firmware template, and original profile PR | Do not promote a webshop-internal SKU or configurable ESPHome node name to canonical or alias. Identify custom firmware in the product name when it can affect power use. Follow the concise Zemismart guidance below. |
| Aqara / Xiaomi | Printed manufacturer model codes such as `LEDLBT1-L01`, `LB-L03E`, `SP-EUC01`, and `ZNLDP12LM` | Official Aqara specifications, manual, declaration or compliance table; product label; then HA Device Info and Aqara's official device list | `lumi.*`, numeric Matter product IDs, and HomeKit model strings are normally discovery aliases rather than canonical product codes. Follow the detailed Aqara guidance below. |
| Arlec | Printed individual-device codes such as `ALD295HA`, `GLD130HA`, and `PC191HA` | Official Arlec product page and specifications, product or packaging label, then HA Device Info and the original profile PR | Pack suffixes such as `P3` and `P5` normally identify a package SKU rather than the physical device, but retain them as aliases when HA can report them. Follow the detailed Arlec guidance below. |
| Athom | Hardware article/model codes such as `LB01-7W-E27` and `PG05V2-AU10A` | Athom product page or label, official Athom firmware configuration, original profile PR, then ESPHome Devices as corroboration | ESPHome project names are split into manufacturer and model by Home Assistant. Preserve the exact resulting values rather than the full dotted project name. Follow the detailed Athom guidance below. |
| Bang & Olufsen / Beoplay | Preserve the exact official product name, such as `Beoplay M5` or `Beosound Edge`, when no printed manufacturer model/type number has been verified | Product label/manual and official Bang & Olufsen support page; then exact HA Device Info and the originating integration | Older speakers can be exposed with values such as `2714 CA16` or `6661 S53`; preserve confirmed exact values as discovery aliases, not automatic canonical article numbers. Follow the detailed Bang & Olufsen guidance below. |
| Bosch | Official device type designations such as `BSHC-1`, `BSP-EZ`, and `BSP-FZ` | Official Bosch product data and manuals; then HA Device Info and the Bosch local API | Values such as `SmartHomeController` and `PLUG_COMPACT` are discovery identifiers rather than preferable canonical type codes. Follow the concise Bosch guidance below. |
| Bose | Official Bose type/model designations such as `416776` and `421650` | Bose owner guides and declarations; then HA Device Info and the SoundTouch device API | Regional product numbers such as `767520-2100` are not preferable canonical IDs. Preserve exact SoundTouch type names and `Bose Corporation` as discovery aliases. Follow the concise Bose guidance below. |
| Calex | Official numeric product codes such as `5101002000` | Official Calex product page, declaration and product label; then exact HA Device Info | Calex publishes the product code and EAN separately. Do not treat the product code as a barcode. Follow the concise Calex guidance below. |
| Cree | Official ordering/model codes such as `BA19-08027OMF-12CE26-1C100` and `CMA19-60W-AL-9TW-GL` | Official Cree documentation, product label, and technical retailer data; then HA Device Info and Zigbee/Tuya diagnostics | Amazon ASINs are retail identifiers. Preserve exact descriptive Zigbee model strings and opaque Tuya model values as discovery aliases when mapped to the measured bulb. Follow the concise Cree guidance below. |
| Dreame / Dreametech | Official radio-equipment or printed type codes such as `RLS3D` and `RLX85CE-5` | Dreame declarations, manuals, and product labels; then exact HA Device Info from the Dreame Vacuum integration | Values such as `dreame.vacuum.r2205` and `dreame.vacuum.r2538z` are integration discovery identifiers. Product families such as X50 can cover several robot and base-station variants. Follow the concise Dreame guidance below. |
| Dreo | Exact device model codes such as `DR-HAF001S` and `DR-HTF008S` | Dreo security, support, and product pages; product label/manual; then the official Dreo HA integration and current Device Info | The integration's `deviceName` is user-facing and not a model alias. A grouped sibling code such as `DBTF08S` is not an alias without evidence of electrical equivalence. Follow the concise Dreo guidance below. |
| EGLO | Official numeric material numbers such as `110288`, `900053`, `900084`, and `98847` | EGLO technical data sheet and product label; then exact HA Device Info and the original profile PR | AwoX model values such as `33955` and `33957` can be shared by electrically different EGLO products. Preserve confirmed values as discovery aliases and let PowerCalc offer multiple candidates. Follow the concise EGLO guidance below. |
| Elgato | Printed or regulatory hardware model codes such as `20GAK9901`, `20LAB9901`, `20LAA9901`, `20LAC9901`, and `20LAG9901` | Device label and Elgato/Corsair support or regulatory documentation; then exact HA Device Info and the original profile PR | `10...` values can be retail SKUs, while HA often reports a product string such as `Elgato Key Light`. Follow the concise Elgato guidance below. |
| Elgin | Complete printed product codes such as `48BLEDWIFI00`, `48BLED15WIFI`, and `48LSB30WIFI0` | Product label and original profile PR; then official Elgin product data and exact Tuya/Tuya Local diagnostics | Opaque Tuya model values can be discovery aliases when confirmed for the measured device. Never infer them or add generic `Tuya` as a manufacturer alias. Follow the concise Elgin guidance below. |
| eQ-3 / Homematic | Official short designations such as `HM-LC-Sw1-DR`, `HmIP-BSM`, and `HmIP-FSM16` | Official eQ-3 or Homematic IP downloads, product sheet and label; then exact HA Homematic Device Info | Numeric item numbers are order references, while HA can change only the casing of an `HmIP-...` code. Follow the concise eQ-3 guidance below. |
| Epson | Product model designations such as `ET-3760` and `L6270` | Official Epson product/support page and manual; then HA IPP Device Info and the original profile PR | IPP can append ` Series` to the model. Preserve that exact reported value as an alias, but do not automatically promote a regional `C11...` SKU. Follow the concise Epson guidance below. |
| Eufy | Official product codes such as `T8400` and `T8410` | Official Eufy product/support compatibility tables and manuals; then exact HA Device Info and the original profile PR | Longer values such as `T84001W1` can be regional retail SKUs. Preserve `Eufy Security` as a manufacturer alias when HA reports it. Follow the concise Eufy guidance below. |
| Eve | Official `20...` model numbers such as `20EBU4101` and `20EBO8301` | Eve's official accessory-identification page and product label; then exact HA Matter Device Info | Eve distinguishes `10...` product numbers from `20...` model numbers. A numeric Matter product ID can be a discovery alias only with exact HA evidence. Follow the concise Eve guidance below. |
| Everspring | Official model codes such as `AN158` | Official Everspring documentation, Z-Wave JS device config, and exact HA Z-Wave JS Device Info | Regional suffixes such as `AN1582` and `AN1586` can identify different plug variants. Do not infer them as aliases. Follow the concise Everspring guidance below. |
| eWeLight / eWeLink | Exact printed product codes or specific integration models such as `SA-003-Zigbee` and `EZ-E14`; retain an established FCC ID only as a manual library ID when no unique product code is available | Product or packaging label, regulatory exhibits, exact HA Device Info, a product-specific Zigbee device record, and the integration's raw Zigbee signature | Keep `eWeLight` and `eWeLink` separate. A raw Zigbee manufacturer value alone does not establish eWeLink as the product brand, but a product-specific record can. Generic light identifiers such as `ZB-CL01` and `ZB-RGBCW` can cover multiple physical products and are unsafe aliases. Follow the concise combined guidance below. |
| Forever Light | Printed product codes such as `FLSP10A` | Product or packaging label, official Forever/TelforceOne data, exact HA Tuya Device Info, and the original profile PR | Retain an exact opaque Tuya product ID as an alias when the measured product establishes the mapping. |
| Fibaro | Official model family codes such as `FGWP-102` | Official FIBARO manual and product label, Z-Wave JS device config, and exact HA Z-Wave JS Device Info | Z-Wave JS may report `Fibargroup` as manufacturer. Preserve an established hyphenless value such as `FGWP102` as an alias and legacy ID when migrating. Follow the concise Fibaro guidance below. |
| Free | Exact Freebox API model values such as `Freebox v8 (r1)` and its separate model ID such as `fbxgw8-r1` when no printed hardware code is established | Official Free product/support material, the Freebox API response, exact HA Device Info, and the original profile PR | Keep model and model ID as separate discovery values. Use the official marketing role in `name`, such as `Server Pop` or `Player Pop`. Follow the concise Free guidance below. |
| GE | Official GE Link product codes such as `PSB30-SW27` and `PSB38-BW30` | Product label, regulatory filing, official compatibility documentation, and exact HA ZHA Device Info | Legacy GE Link bulbs may report `GE_Appliances` and the shared model `ZLL Light`. Preserve those exact discovery values and let PowerCalc offer the confirmed candidates. Follow the concise GE guidance below. |
| Genio | Mirabella Genio item numbers such as `I002741` and `I004544` | Official Mirabella product page, product label, regulatory record, and original profile PR | Tuya may expose only a generic model and `Tuya` as manufacturer; do not add those broad values as Genio discovery aliases. Follow the concise Genio guidance below. |
| Hifree | Printed model codes such as `T21` | Official manual or regulatory filing, product label, the exact retail package from the original PR, and exact HA Tuya Device Info | Tuya can expose an opaque `product_id`; retain that exact value as an alias, not as the canonical model. Tie power and barcode metadata to the measured retail revision because `T21` listings can differ. |
| Gledopto | Exact printed and Zigbee model codes such as `GL-B-003P`, `GL-D-004P`, and `GL-S-007Z` | Official Gledopto product page, manual or catalog; product label; then exact HA ZHA/Zigbee Device Info | Preserve suffixes such as `P`, `Z`, and `S`. Some model codes were reused across capability or firmware revisions. Follow the concise Gledopto guidance below. |
| Google | Official hardware model numbers such as `H0ME`, `H0A`, `H2C`, `H2A`, `GUIK2`, `GXCA6`, and `H2E` | Google regulatory and ecodesign documentation; product label; then exact HA Cast Device Info and established PowerCalc discovery values | Preserve the established unprefixed discovery model as an alias when replacing a marketing-name directory. Follow the concise Google guidance below. |
| Gosund | Printed product codes such as `SL2` | Product label or manual; then exact HA Device Info and established ESPHome or Tasmota device catalogs | Preserve the manufacturer's capitalization. A casing-only discovery difference does not require an alias because PowerCalc matches models case-insensitively. Follow the concise Gosund guidance below. |
| Govee | Official `H...` product model codes such as `H600D`, `H607C`, and `H7020` | Official Govee product page and developer model catalog; then exact HA Device Info and the original profile PR | Matter can report manufacturer `Shenzhen Qianyan Technology` and a numeric `model_id` separately from the `H...` model. Follow the concise Govee guidance below. |
| GreenWave | Prefer the exact printed regional PowerNode code, such as `NS210` or `NS310`, when it is known for the measured device; otherwise retain the established `GWPN1` discovery-family ID | Product label and GreenWave manual; exact HA Z-Wave JS Device Info; then Z-Wave device databases and the original profile PR | `GWPN1` can cover multiple one-port hardware variants. Do not guess a printed code or add every compatible variant as an alias. Follow the concise GreenWave guidance below. |
| Hampton Bay | Exact printed model codes such as `HB-10521-HS` | Official Hampton Bay/Home Depot product page and use-and-care guide; product label; then exact HA Hubspace Device Info and the original profile PR | Do not infer `Hubspace` or the underlying supplier as a manufacturer alias. Follow the concise Hampton Bay guidance below. |
| Harman Kardon | Prefer a verified printed article or model code; otherwise retain established Cast model strings such as `HK Citation MultiBeam 700` and generation-specific profile IDs such as `HK Citation One MKII` | Official product page, service manual or product label; then exact HA Cast Device Info and the original profile PR | Cast can omit the product generation and report a shared value such as `HK Citation One`; preserve a confirmed shared value as a discovery alias. Follow the concise Harman Kardon guidance below. |
| Hornbach / FLAIR Viyu | Official Hornbach article numbers such as `10454469` | Official Hornbach product page, compatibility list, product instructions or label; then exact HA Zigbee Device Info and the original profile PR | Preserve a confirmed compound Zigbee model such as `VIYU_A60_806_CCT_10454469` as a discovery alias. Follow the concise Hornbach guidance below. |
| HP | Exact HP product model or established IPP model values such as `MFP 3301` | Product label, official HP user guide and support page; then exact HA IPP Device Info and the original profile PR | IPP provides `model` but no separate `model_id`; exclude serial numbers and UI-only suffixes. Follow the concise HP guidance below. |
| Inovelli | Exact Inovelli SKU codes such as `VZM31-SN` and `VZM32-SN` | Official Inovelli device manual and product label; then exact HA Zigbee Device Info and the raw Zigbee signature | Use the official series/product description for `name` without repeating the SKU. Follow the concise Inovelli guidance below. |
| Intelbras | Exact printed product codes with manufacturer spacing, such as `EWS 410` | Intelbras product label, datasheet and official product page; then exact HA Device Info and integration diagnostics | Generic `Tuya` Device Info is not an Intelbras manufacturer alias. Do not infer electrical equivalence from a combined product page. Follow the concise Intelbras guidance below. |
| Kobi | Official catalog indices such as `KAGS14RGBCTW` | Official KOBI catalog, product label, exact HA Tuya Device Info, and the original profile PR | Preserve KOBI branding; do not turn an historical `Ekobi` spelling into a manufacturer alias without Device Info evidence. |
| JBL | Official product numbers such as `LINK MUSIC`; preserve an established casing-only directory when discovery and migration do not benefit from a rename | JBL declarations, specification sheets and product pages; then exact HA Cast Device Info and the original profile PR | Cast can report a manufacturer-prefixed model such as `JBL Link Music` and manufacturer `HARMAN International Industries`; keep those exact discovery values. Follow the concise JBL guidance below. |
| Kauf | Printed bulb model codes such as `BLF10` | Product housing and official Kauf product or firmware documentation; then exact HA ESPHome Device Info and the original profile PR | Kauf firmware can expose the generic model `RGBWW` for different bulb sizes and wattages. Do not use it as an alias. Follow the concise Kauf guidance below. |
| Kogan | Exact Kogan product codes such as `KASB10WB22HA` | Official Kogan manual, support page and product label; then exact HA Device Info and the original profile PR | Fitting and pack variants have separate codes. Do not add generic Tuya values or sibling codes as aliases. Follow the concise Kogan guidance below. |
| LEDVANCE / OSRAM | Prefer the exact official manufacturer article/model code, such as an `AC...` code or an older OSRAM article number. Retain a device-reported Zigbee model such as `CLA60 RGBW Z3` as canonical only when no more specific official article code can be established for the measured hardware. | Official LEDVANCE product datasheet, declaration or catalog; product label; then HA/integration diagnostics and Zigbee signature | Exact protocol model strings, integration-specific IDs, and officially mapped GTINs can be aliases. Follow the detailed LEDVANCE guidance below. |
| Ledworks / Twinkly | Exact Twinkly base product SKU such as `TWS600STP` | Official Twinkly product/help documentation; then exact HA Device Info and the original profile PR | Preserve capability suffixes: `STP` is Multicolor/RGB while `SPP` adds white. Regional suffixes are not aliases unless HA reports them. Follow the concise Ledworks guidance below. |
| Lenovo | Exact Lenovo hardware model such as `CD-24501F` | Official Lenovo product support and open-source pages; then exact HA Cast Device Info and the original profile PR | Cast product names such as `Lenovo Smart Clock` are discovery aliases, not substitutes for the generation-specific hardware code. Follow the concise Lenovo guidance below. |
| Lexman | Exact supplier model identifier and Zigbee model, such as `IG-CDZOTAAG014RA-MAN` | Official ADEO product-information sheet; then exact HA/Zigbee Device Info and the original profile PR | Keep ADEO as a manufacturer alias when reported by Zigbee. Leroy Merlin article references are not discovery aliases unless HA reports them. Follow the concise Lexman guidance below. |
| Innr | Printed and device-reported codes such as `RB 285 C`, `RS 230 C`, `SP 240` | Official Innr product page or declaration, product label/manual, then HA ZHA/Zigbee device signature | Keep spaces and suffix letters when they are part of the model code. Bundle SKUs and regional sibling models are not aliases by default. Follow the detailed Innr guidance below. |
| Lidl | Printed product/type codes such as `HG06462A`, `HG08131B`, a specific article code, or an IAN-based identifier when that is the only stable label code | Product or packaging label/manual, official Lidl documentation, then current HA Device Info and the integration's raw Zigbee signature | Reject generic Zigbee models such as `TS0502A` and `TS0505B`. Do not assume a raw `_TZ...` string is a manufacturer alias. Follow the detailed Lidl guidance below. |
| Lightinginside | Product-specific printed model codes such as `LM-MT-C2-A19-9W` and `LI-A19-11W-MT` | Product or packaging label and listing; then exact HA Matter Device Info and the original profile PR | Matter model ID `1718` and model `Smart LED Bulb` can be shared by different wattages. Follow the concise Lightinginside guidance below. |
| Lindby | Exact Lindby model identifier or article number, such as `9971013` | Lindby EPREL product-information sheet or product label; then exact HA Device Info and the original profile PR | Generic Tuya values such as manufacturer `Tuya` and model `bulb RGBW` do not uniquely identify a Lindby product. Follow the concise Lindby guidance below. |
| Linkind | Exact printed Linkind product codes such as `LS0101911261`, `LS0102411267`, and `ZL1000701-27-EU-V1A02` | Product or bulb label and official Linkind product page; then exact HA Matter Device Info, Zigbee device mapping, and the original profile PR | Preserve confirmed Matter product IDs and exact Zigbee model strings as discovery aliases. Follow the concise Linkind guidance below. |
| LSC | Official LSC or Electro Cirkel model/article codes such as `970710` and `970714`; retain a specific Action article code when it is the only verified product identifier | LSC/Electro Cirkel declaration, EPREL, product label, Action product page, and the original profile PR | Action product numbers and factory model strings are separate identifiers. Preserve an exact integration-reported factory model as an alias, but do not infer aliases from sibling revisions. Follow the concise LSC guidance below. |
| Luedd | Official five-digit Luedd model/article numbers such as `02626` | Luedd product page, product-information sheet and product label; then exact HA Device Info and the original profile PR | Tuya manufacturer and model values are discovery data only. Preserve an established descriptive model as an alias when migrating. Follow the concise Luedd guidance below. |
| Lumiman | Printed Lumiman product codes such as `LM530`, qualified by the measured fitting or regional variant where available | Product label, FCC filing and original profile PR; then exact HA Device Info and Tuya diagnostics | Generic LocalTuya values such as manufacturer `Tuya` and model `LUMIMAN SMART BULB` are not safe discovery aliases. Follow the concise Lumiman guidance below. |
| Lutron | Exact Lutron model numbers such as `PD-3PCL-WH` | Official Lutron product page, specification and product label; then exact HA Lutron Caséta Device Info | HA combines the device model and type as `<model> (<type>)`; keep the type such as `PlugInDimmer` as the separate alias. Follow the concise Lutron guidance below. |
| Malmbergs | Official Malmbergs article numbers such as `9970020` | Official Malmbergs product page and product label; then exact HA Device Info and the original profile PR | Keep the product-family wording in `name`; add a discovery alias only when an integration is confirmed to report it. Follow the concise Malmbergs guidance below. |
| Mammotion | Official product-model names such as `YUKA mini 600` | Official Mammotion product page and product label; then exact HA Device Info from the Mammotion integration and the original profile PR | Do not combine identical `model` and `model_id` values into a parenthesized alias. Follow the concise Mammotion guidance below. |
| MeLiTec / LightZone | Printed MeLiTec model codes such as `DP15` | Product type plate and official MeLiTec documentation; then exact ESPHome/HA Device Info and the original profile PR | `LightZone` is a product brand and established discovery manufacturer alias. Keep separate article, design and EAN values out of model aliases unless HA reports them. Follow the concise MeLiTec guidance below. |
| Mercator Ikuü | Official SKU codes such as `SMD4109W-RGB-ZB` | Official Mercator/Ikuü product page and data sheet; then exact HA Device Info, raw Zigbee signature, converter mapping and original profile PR | A generic model such as `TS0505B` is safe only in combination with the exact `_TZ...` manufacturer values confirmed for the measured product. Follow the concise Mercator Ikuü guidance below. |
| Meross | Official product codes such as `MSL120` | Official Meross product page and manual; then exact HA Device Info and the original profile PR | Lowercase values with a revision suffix, such as `msl120d 2.0.0`, can be Meross integration-reported discovery identifiers rather than printed product models. Follow the concise Meross guidance below. |
| MOES | Complete official webshop SKUs such as `MWB-TD5-RCW-GU10-MS` and `ZB-LZD10-RCW-EN` | Official MOES product page and variant metadata; then exact HA Matter/Zigbee Device Info and original profile PR | Numeric Matter product IDs and shortened Zigbee model values belong in aliases. Preserve SKU suffixes and exclude multipack SKUs. Follow the concise MOES guidance below. |
| Müller Licht / tint | Official article numbers printed on the product or shown as `Artikel-Nr.`, such as `404005`, `404094`, and `404095` | Official Müller Licht product-information page and product label; then exact HA Device Info and original profile PR | Generic Zigbee values such as `ZBT-ColorTemperature`, `ZBT-ExtendedColor`, and `tint-ExtendedColor` can cover materially different products. Follow the concise Müller Licht guidance below. |
| Nanoleaf | Official Nanoleaf model/SKU codes such as `NL67E200` and `NF080B03-1A19E` | Nanoleaf product page or support model-family table, regulatory product sheet and product label; then exact HA Matter Device Info and original profile PR | Matter product names and decimal product IDs such as `Essentials A19/A60` and `67` are discovery aliases and can be shared by regional variants. Follow the concise Nanoleaf guidance below. |
| Netatmo | Exact printed or regulatory product models such as `NHC01` | Official Netatmo declaration or product label; then exact HA Netatmo Device Info and original profile PR | Home Assistant can report the current product name instead of the hardware model; preserve that exact name as a discovery alias. Follow the concise Netatmo guidance below. |
| NETGEAR | Exact printed product models such as `GS110MX` | Official NETGEAR product page or datasheet and product label; then the original profile PR | Unmanaged switches without a native HA integration can be manual-only profiles and do not need invented discovery aliases. Follow the concise NETGEAR guidance below. |
| Neo CoolCam | Exact printed product models such as `NAS-WR01Z` | Product manual or label; then exact HA Z-Wave Device Info, original measurement discussion and profile PR | Keep the exact Z-Wave manufacturer value `Shenzhen Neo Electronics Co., Ltd.` as a discovery alias. Follow the concise Neo CoolCam guidance below. |
| NodOn | Exact printed product codes when known; otherwise a certified Z-Wave family identifier such as `MSP-3-1-X1` | Official NodOn manual or declaration, Z-Wave certification catalog, exact HA Z-Wave Device Info and original profile history | `ID-RF` is the legacy legal manufacturer reported by Z-Wave. Do not infer a regional socket suffix from the family identifier. Follow the concise NodOn guidance below. |
| NOUS | Official A-series product codes such as `A1T` and `A1Z` | Official NOUS product page and manual; then exact HA Device Info, protocol fingerprint and original profile PR | Preserve exact firmware- or protocol-reported discovery values, but pair generic Zigbee models with the confirmed manufacturer signature. Follow the concise NOUS guidance below. |
| Nuki | Official manufacturer SKUs such as `220694` | Official Nuki shop URL/product page and product label; then exact HA Device Info and original profile PR | Preserve `Hardware Bridge` as the integration-reported discovery alias and exclude the serial number. Follow the concise Nuki guidance below. |
| Oz Smart Things | Exact model codes printed on the device or packaging, such as `DL41-03-10-R-ZB` | Product or packaging label and original profile PR; then the official product page and exact HA Device Info | Do not map a current V2 webshop SKU to an older measured model without evidence that the hardware and power behavior are unchanged. Follow the concise Oz Smart Things guidance below. |
| Philips Air Care | Exact regional model codes such as `AC4236/14`, represented with an underscore only in the directory name when required by the filesystem | Product or packaging label, official Philips manual or product page, then exact HA Device Info from `philips_airpurifier_coap` | Preserve the slash-form model as an alias for discovery. Do not infer sibling regional suffixes or treat Philips air-care devices as Signify / Philips Hue products. Follow the concise Philips Air Care guidance below. |
| Qubino | Exact ordering/model codes such as `ZMNHYD1` | Official Qubino technical specifications and manual, product label, then exact HA Z-Wave JS Device Info and device fingerprint | Z-Wave JS can expose the product label `Smart Plug 16A` for the shared fingerprint; preserve that exact value as a discovery alias. Do not infer sibling frequency codes. Follow the concise Qubino guidance below. |
| Reolink | Official item/model codes such as `E330` and `RLC-810WA` | Reolink quick-start guide, product/support page and device label; then exact HA Reolink Device Info and original profile PR | HA can combine a marketing name and item code as `E1 Pro (E330)`; preserve that exact value as a discovery alias. Hardware-version strings such as `IPC_...` are not automatic aliases. Follow the concise Reolink guidance below. |
| Rituals | Official article/trade codes such as `1106834` | Official Rituals catalog or product URL and product label; then exact HA Device Info from `rituals_perfume_genie` | Preserve `Rituals Cosmetics` and `The Perfume Genie 2.0` as exact discovery aliases. LOT numbers are device-specific and must not be aliases. Follow the concise Rituals guidance below. |
| ROBB | Exact `ROB_...` product and Zigbee model codes such as `ROB_200-017-0` | ROBB product page or label, then exact HA Device Info and deCONZ/Zigbee2MQTT device data | Preserve `ROBB smarrt` as the integration-reported manufacturer alias. Do not infer sibling revision codes such as `-1`. Follow the concise ROBB guidance below. |
| Roborock | Exact printed product model such as `SDJQR01RR`, `S5E02-00`, or `S6V52-00` | Official Roborock/Xiaomi support data and product label; then exact HA Roborock or Xiaomi Miio Device Info and the original profile PR | Preserve internal models such as `rockrobo.vacuum.v1`, `roborock.vacuum.s5e`, and `roborock.vacuum.a10` as discovery aliases. Verified color-only SKUs may share one profile. Follow the concise Roborock guidance below. |
| Rye | Exact printed product model such as `SLZC3705-A` | Product label or an exact hardware-backed device template, then exact HA Device Info and the original profile PR | Preserve the released `Candlebra` identifier and exact Tasmota template name `RYE Candlebra`; do not add generic firmware manufacturer `Tasmota` without exact Device Info. Follow the concise Rye guidance below. |
| Sengled | Exact printed and raw Zigbee models such as `E11-G13` and `E13-N11` | Official Sengled manual or product label, exact HA Device Info, Zigbee converter definition and original profile PR | A Zigbee2MQTT display model can intentionally group multiple raw model values; preserve the grouped value and each exact member as discovery aliases. Follow the concise Sengled guidance below. |
| WiZ | Prefer the exact WiZ/Signify material or article number (12NC), commonly beginning with `929...`; retain another verified printed manufacturer model when no 12NC can be established | Official WiZ product specifications, product or packaging label, then HA WiZ Device information and the raw `moduleName` | `SHRGB`, `SHRGBC`, `SHTW`, and related `SH...` values are module/configuration codes rather than unique article numbers. Keep confirmed values as intentionally shared discovery aliases. Follow the detailed WiZ guidance below. |
| Sonos | Prefer the exact manufacturer article/model number printed on the product or packaging when it uniquely identifies the measured hardware. Preserve established product-name directories unless a verified canonical migration is intentionally requested. | Product label/packaging and official Sonos documentation; then HA Sonos Device information, where `model_id` comes from the speaker's `modelNumber` | `S...` values are useful discovery aliases when one-to-one with the measured generation, but are not automatically retail article numbers. Product names and S-codes can both be generation-ambiguous. Follow the detailed Sonos guidance below. |
| SONOFF | Printed product codes such as `B1`, `B02-B-A60`, `S20`, `ZBMINI`, and `ZBMINIR2` | Official product page/manual and product label, then exact eWeLink or HA Device Info and Zigbee device definitions | Preserve punctuation in the official model. Retain confirmed integration or raw Zigbee values as aliases, but do not substitute a generic Tuya identifier. Follow the concise SONOFF guidance below. |
| Sony | Exact printed model codes such as `LF-S50G` | Official Sony support page/manual and product label; then exact HA Cast Device Info and original profile evidence | Keep the official product description in `name` without repeating the model code or manufacturer. Follow the concise Sony guidance below. |
| Sunco | Exact official SKU such as `G25_S-5W-27K_5K-1PK` when the original contribution identifies that retail product | Product label, exact official Sunco product page, exact HA Tuya Device Info, and the original profile PR | A pack suffix is acceptable when the original PR maps the measured item to that exact official product; preserve the former Tuya model as an alias. Do not infer a SKU from a lookalike page. |
| Sylstar | Exact retail or printed product model such as `SL-CL-24` | Product label, the exact original retail listing, official Sylstar documentation, exact HA Tuya Device Info, and the original profile PR | Preserve an exact integration-reported value such as `SYS-C24W-CW` as an alias when the original contribution maps both codes to the measured fixture. |
| Sylvania | Exact model codes printed on the lamp, such as `A19CCECWF` and `40A19FILCCLWIFI` | Lamp label, official LEDVANCE/Sylvania catalog, then exact Tuya product data and original profile evidence | A Tuya cloud product ID can be retained as an alias, but LocalTuya may expose only a device-unique ID. Stock-firmware LUTs are not automatically valid after reflashing. Follow the concise Sylvania guidance below. |
| Tasmota | Exact Tasmota module or active-template names that uniquely map to the underlying hardware | Exact HA Tasmota Device Info and active Tasmota template/module; then official hardware documentation | Tasmota is firmware, not the hardware manufacturer. Require a confirmed module-to-hardware mapping for new routes, but preserve released compatibility routes unless a safe migration exists. Follow the concise Tasmota guidance below. |
| Teckin | Printed retail model codes such as `SB50` | Official Teckin product page/manual and product label, then original profile evidence | Preserve the measured firmware context. Teckin may reuse a retail model across hardware revisions, so do not copy current catalog specifications into an older profile without confirming the measured revision. |
| Telefunken | Full model code from the TV label, including suffixes such as the `M` in `XF32E411M` | TV label and original measurement evidence, then a matching product datasheet/manual | Do not collapse a suffixed retail model into the shorter family code or add that family code as an alias without evidence that Home Assistant exposes it and the hardware is equivalent. |
| Third Reality | Exact Zigbee model exposed by HA, such as `3RCB01057Z`, combined with exact product evidence | Exact HA Device Info, official product manual and packaging barcode; then Zigbee device definitions | Keep `Third Reality, Inc` as a manufacturer alias. Model codes can be reused across product forms, so distinguish revisions with exact product codes and barcodes. Follow the concise Third Reality guidance below. |
| Tuya and white-label devices | Use the real consumer brand and its printed model when verified; retain Tuya only for genuinely unbranded or unresolved hardware | Product and packaging label, official brand documentation, exact HA Tuya Device Info, and the original profile PR | Identifiers such as `TS0601`, `TS0505B`, and `_TZE...` values can cover different hardware. A compatibility alias is safe only when the complete old manufacturer/model pair resolves uniquely. Follow the concise Tuya guidance below. |

## Amazon

Apply these rules when reviewing or normalizing Amazon Echo and Alexa profiles:

1. Prefer the exact physical hardware model printed on the device or established by official or regulatory documentation as the canonical directory ID. Do not use an Amazon Standard Identification Number (ASIN), commonly a ten-character retail code beginning with `B0`, as the canonical model merely because it appears on a product page or order. Add an ASIN to `aliases` only when an integration is also confirmed to report it.
2. Preserve exact Alexa device types such as `A2U21SRK4QGSE1` and `A18O6U1UQFJ0XK` as discovery aliases when they map to the measured hardware. Alexa Media Player may expose a fallback model composed as `<deviceFamily> <deviceType>`, producing values such as `ECHO ...` and `KNIGHT ...`; retain the exact prefixed value when Device Info, diagnostics, integration source, or the original PR confirms it.
3. Amazon serial numbers or device serial numbers (DSNs) are an intentional exception to the universal alias exclusion when the exact value already exists as an Amazon profile alias or is confirmed as a model value required for Home Assistant discovery. Keep it only on the profile for the device from which it was captured, never use it as the canonical directory ID, and never copy or generalize it to sibling profiles. A stable Alexa device type should be added alongside it when available, rather than replacing an established discovery alias.
4. Check every alias against the canonical IDs and aliases of other Amazon profiles. Similar Echo models can have distinct physical codes, such as clock and non-clock variants, and an alias must not point to a sibling profile merely because they share a generation or enclosure.
5. Preserve a verified regional or hardware model variant when the contributor confirms the exact printed label. If an existing code differs by one character from independently documented codes, do not silently normalize it; ask for a label photo or exact transcription before correcting it.
6. Alexa speakers may reject the measurement tool's directly streamed test MP3 and play a spoken error instead. Confirm that pink noise or another continuous, documented test signal was actually playing while volume levels were measured. For older profiles created before the tool could disable direct streaming, ask whether playback was started manually, for example through Spotify, when the curve or PR evidence is inconclusive.
7. Use concise product names with consistent generation formatting, such as `Echo Dot (Gen3)` and `Echo Show 8 (Gen2)`. Distinguish variants such as `with clock` only when they represent different physical models and profiles.

When an Amazon hardware code, ASIN, Alexa device type, family-prefixed model, or serial discovery value cannot be reconciled, keep the established profile available and ask for the product-label code, exact Home Assistant Device Info fields, originating integration, and measurement playback method.

## Apple

Apply these rules when reviewing or normalizing Apple HomePod profiles:

1. Prefer Apple's printed `A...` hardware model number as the canonical directory ID when official Apple regulatory data or an information guide maps it uniquely to the measured generation. For example, `A2374` identifies HomePod mini and `A2825` identifies HomePod (2nd generation). Do not derive an A-number from a similar product or generation name alone.
2. Distinguish the hardware model number from Apple's regional and color-specific order/SKU codes, commonly beginning with `M`, and from internal identifiers such as `AudioAccessory5,1` and `AudioAccessory6,1`. A code such as `MQJ83` identifies an orderable HomePod variant rather than the generation-wide hardware model, so it is not preferable as the canonical directory ID.
3. Home Assistant's Apple TV integration normally exposes the pyatv `model_str` result as the Device Info model. Preserve the exact reported strings, including capitalization, as aliases: current mappings include `HomePod Mini` and `HomePod (gen 2)`. Treat a raw `AudioAccessory...` value as an alias only when Device Info or diagnostics show that Home Assistant actually exposed it, because Home Assistant uses the raw value only when pyatv cannot identify the model.
4. Add an established marketing name or SKU to `aliases` only when it is a genuine discovery value. For example, pyatv reports `HomePod Mini`, while an order code such as `MQJ83` should not be an alias when Device Info instead reports `HomePod (gen 2)`.
5. Do not add every color or regional SKU as an alias merely because those variants share the same A-number. A cosmetic variant may share the profile when the hardware model and electrical behavior are the same, but its order code is useful for discovery only when an integration reports that exact code.
6. Use Apple's product capitalization in the display name, notably `HomePod mini`. Keep generation formatting consistent with the library, while preserving the integration's exact casing separately in `aliases` when it differs.
7. Verify speaker measurement provenance in the original PR. If the contributor manually changed volume, started pink or white noise, and used the utility only to average each level, set `measure_method` to `manual`, describe the signal, increments, and averaging duration, and remove script settings that did not control the playback or level sequence.

When the A-number, order code, internal identifier, and Home Assistant model cannot be reconciled, keep the established profile available and ask for the bottom-label model number, exact Device Info model, originating integration, and product generation before renaming or adding aliases.

## 3A Smart Home / Nue

Apply these rules when reviewing or normalizing 3A Smart Home and Nue profiles:

1. Treat device-reported codes such as `LXN60-LS27-Z30` and `LXT56-LS27LX1.7` as Zigbee hardware evidence, not automatic proof of the complete product's canonical article code. In particular, an `LXT...` controller identifier can be exposed by both a complete downlight and a standalone RGBW strip controller.
2. Prefer the exact product-specific code printed on the complete light, its supplied driver, dimmer, or packaging when it can be verified against the measured product. `WL-SD001-9W` is confirmed from the measured 9 W downlight's packaging and supersedes its former `LXN60-LS27-Z30` directory ID. `HGZB-DLC4-N12B` is confirmed from the measured 12 W RGBW downlight's driver together with its 720–800 lm output and 90 mm cut-out; do not substitute an inferred sibling code such as `WL-SD001-12W`.
3. Retain an existing `LXN...` or `LXT...` value in `aliases` when Home Assistant or the originating integration actually reports it, and add it to `legacy_ids` when it was the former profile directory. A confirmed shared controller alias may occur on multiple profiles so PowerCalc can offer the matching candidates; never use that shared alias alone to merge measurements or claim hardware equivalence.
4. Preserve complete suffixes and revisions such as `.1` and `.3`. Do not assume sibling controller codes identify equivalent dimmers or share self-usage merely because the base code and product description match.
5. Accept `3A Smart Home DE` and other manufacturer aliases only when the exact value appears in Home Assistant Device Info for the originating integration. Do not generate capitalization variants solely for completeness or infer aliases from a raw signature without confirming how Home Assistant exposes it.
6. Use the branded product line, such as `Nue`, and the verified product type in `name`; omit the generic word `Smart`, the protocol name `ZigBee`, and the manufacturer name when they add no product distinction. Format ratings with a space, for example `Nue RGBW Downlight (9 W)`.
7. Keep connectivity tied to the measured hardware revision rather than a current page for the same retail code. The measured `WL-SD001-9W` profile is the Zigbee revision; do not replace that with Wi-Fi/Bluetooth metadata from a later or parallel product page.

When a controller identifier and apparent product code cannot be reconciled, keep the established profile unchanged and ask for a product-label or packaging photo plus the exact Home Assistant manufacturer, model, originating integration, wattage, and relevant physical specifications.

## Antela

Keep connectivity tied to the measured product and its exact Tuya evidence. The established `ANTELA-9W-RGB+CCT` bulb uses Wi-Fi; do not infer that a similarly named retail revision uses the same transport without exact package or Device Info evidence.

## Anko / Kmart

Use Kmart's numeric article number as the canonical Anko model only when the exact product manual or packaging prints that number. For integrated decorative lights, record the complete luminaire input rating from the exact manual as `rated_power`; do not invent a lumen value when the manual specifies only input power, dimensions, and LED count.

## Aqara / Xiaomi

Apply these rules when reviewing or normalizing Aqara profiles:

1. Prefer the exact manufacturer model code printed on the product or established by official Aqara specifications, manuals, declarations of conformity, or compliance tables as the canonical directory ID. Codes such as `LEDLBT1-L01`, `LB-L01D`, `LB-L03E`, `PS-S02E`, `SP-EUC01`, and `ZNLDP12LM` take precedence over protocol or integration identifiers.
2. Treat stable Zigbee identifiers such as `lumi.light.agl006` and `lumi.plug.maeu01`, numeric Matter product IDs such as `6145` and `6150`, and HomeKit model strings such as `AL039` as discovery aliases. Add them only when Home Assistant Device Info, diagnostics, the original profile PR, or Aqara's official device list maps the exact value to the measured hardware variant.
3. Do not assume that a numeric Matter product ID, a `lumi.*` value, or a marketing name uniquely identifies the physical product. Cross-check fitting, color capabilities, region, and variant; for example, Aqara's E26, E27, and GU10 LED Bulb T2 variants have distinct product codes and protocol identifiers.
4. Where an official product page groups regional suffixes such as `D` and `E`, use the exact variant reported for the measured device as canonical. Add a sibling suffix as an alias only when evidence shows Home Assistant reports it for compatible hardware whose measurements are valid for the profile; a shared product-family listing alone is insufficient.
5. Normalize obvious presentation artifacts only when another authoritative manufacturer source confirms the intended code. For example, a specifications page may render `SP - EUC01`, while an official compliance table establishes `SP-EUC01`. Do not silently repair a conflicting character, suffix, or digit without corroboration.
6. Keep a former integration-derived value in `aliases` when it remains a genuine discovery value, such as a reported `lumi.*` identifier. Do not retain a corrected typo such as `PS-S020E` as an alias.
7. Use Aqara's concise official product description in `name`, omitting the manufacturer. Keep variant details that distinguish hardware, such as fitting and `RGB CCT`, but remove label-style specifications and marketing prose. Add a product name to `aliases` only when the originating integration actually exposes that exact string as its model value.
8. For light metadata, prefer the exact regional model's official specifications and EU EPREL record. Aqara product families can contain different fittings and colour variants, so do not transfer wattage, lumen output, or barcodes between sibling suffixes without an exact model mapping.

When the printed model, protocol identifier, Matter product ID, and Home Assistant model cannot be reconciled, keep the established profile available and ask for the product-label code, exact Device Info fields, originating integration, fitting, region, and light capabilities before renaming or sharing measurements.

## Arlec

Apply these rules when reviewing or normalizing Arlec Grid Connect profiles:

1. Prefer the exact model code of the individual physical device as the canonical directory ID. For example, use `ALD295HA` for the single downlight and `GLD130HA` for the bulb itself rather than a retail multipack SKU.
2. Treat suffixes such as `P3`, `P5`, and `-4` as possible pack quantities, not automatically as part of the device model. Verify their meaning on an official Arlec product page, packaging, or the physical label; do not remove a suffix based on its shape alone.
3. If Home Assistant Device Info, diagnostics, or the original profile PR shows that a pack SKU is reported as the model, add that exact value to `aliases` even though it is not canonical. An alias may intentionally occur on more than one compatible profile when Powercalc must offer the user a choice; do not infer measurement equivalence from the shared alias.
4. Keep a former pack SKU in `aliases` when it remains a real or documented Home Assistant discovery value.
5. Do not add generic Tuya or Grid Connect platform values, raw Tuya manufacturer strings, or `Tuya` as a manufacturer alias merely because the product uses that ecosystem. Require the exact manufacturer and model values exposed by Home Assistant for the measured device.
6. Use concise official Grid Connect product wording in `name`, omitting `Arlec` and cosmetic color. Preserve distinguishing capabilities, fitting, and ratings, with consistent forms such as `RGB+CCT`, `CCT`, `E27`, `B22`, `9W`, `740 lm`, and `92 mm`.
7. For power adaptors, verify whether the exact model has an energy meter before setting `only_self_usage`. Do not transfer that flag between visually similar plugs: metering models can use `only_self_usage: true`, while a non-metering socket or USB charger must use `false`.
8. Do not use a stable Bunnings item number to bridge Arlec hardware revisions. Arlec can replace an older bulb such as `GLD115HA` or `GLD130HA` with `GLD315HA` or `GLD310HA` under the same retail item; transfer specifications or barcodes only when the exact printed device code matches.

When an individual code, pack SKU, or integration-reported value cannot be reconciled, keep the established profile available and ask for the product or packaging label plus the exact Home Assistant manufacturer, model, model ID, and originating integration.

## Athom

Apply these rules when reviewing or normalizing Athom ESPHome and Tasmota profiles:

1. Prefer the exact hardware article or model code as the canonical directory ID, for example `LB01-7W-E27` for the legacy 7W E27 bulb and `PG05V2-AU10A` for the Australian V2 10 A plug. Distinguish the hardware code from a firmware-specific suffix such as `-TAS` or `-ESP`, a retail bundle suffix, and an ESPHome node or project name.
2. Verify that the code identifies the measured generation and region. Athom reuses product pages and firmware configurations across revisions and countries, and an older page can contain a legacy article number alongside specifications for newer hardware. Corroborate conflicting pages with the product label, original PR evidence, electrical specifications, an official firmware configuration, and an established device catalog before migrating a profile.
3. Treat ESPHome's dotted `project.name` as two discovery fields. Home Assistant splits `<manufacturer>.<model>` and exposes the first segment as the manufacturer and the second as the model. For `athom.rgbww-light`, use `athom` and `rgbww-light`; do not add the full `athom.rgbww-light` string as a model alias unless separate Device Info evidence shows it was exposed intact.
4. Preserve both historical and current project-derived discovery values when official Athom firmware changed them. For example, a current project name such as `China Athom Technology.Athom RGBCW Bulb` supports `China Athom Technology` in `manufacturer.json` and `Athom RGBCW Bulb` in the bulb's `aliases`, while the historical model `rgbww-light` remains an alias for older firmware.
5. Keep a former project-derived value in `aliases` when Home Assistant actually reports the same value as the model; do not preserve a purely internal slug as a discovery alias.
6. Keep manufacturer aliases exact. Values such as `Athom_Technology` from original Device Info and `China Athom Technology` from current official ESPHome project names are valid when the corresponding integration exposes them. Do not invent spacing, capitalization, or underscore variants.
7. Use concise product wording in `name`, omit the manufacturer, and retain distinguishing region, generation, fitting, power, and lumen details. Follow Athom's compact wattage style where it identifies the product, for example `7W E27 RGBCCT Bulb for ESPHome (600 lm)` and `Smart Plug AU V2 (10 A)`.
8. Set `only_self_usage: true` only for an exact Athom plug model with a built-in consumption meter. Recheck the hardware revision and regional code because similarly named plugs can use different metering chipsets or current ratings.

When an Athom article code, hardware revision, firmware project name, and Home Assistant Device Info cannot be reconciled, preserve the established profile and ask for the product label, exact region and revision, firmware type, and current Home Assistant manufacturer and model.

## AVM

Record FRITZ!DECT endpoint connectivity as `proprietary`: DECT ULE is the device-side smart-home transport, while the paired FRITZ!Box's Wi-Fi or Ethernet connection is not endpoint connectivity. Do not infer a FRITZ!DECT device's connectivity from the controller or Home Assistant host.

For `rated_power`, use an exact model-bound AVM technical page, manual, or knowledge-base route. Prefer the published maximum when AVM provides both average and maximum consumption; otherwise the exact model's stated total or average consumption is acceptable. Do not reuse a value from document number 138 unless the route and page heading identify the profile's exact model.

## Bang & Olufsen / Beoplay

Apply these rules when reviewing or normalizing Bang & Olufsen speaker profiles:

1. Distinguish the official product name, a printed manufacturer model/type or article code, and the model value reported by an integration. Prefer a verified stable model/type code as canonical when product-label or official manufacturer evidence maps it uniquely to the measured hardware. Otherwise preserve the established official product name as the directory ID rather than promoting an unexplained integration value.
2. Add an exact integration-reported value such as `2714 CA16` for Beoplay M5 or `6661 S53` for Beosound Edge to `aliases` when Home Assistant Device Info, diagnostics, or the original profile PR maps it to the measured speaker. These values preserve discovery but are not proven manufacturer article numbers merely because they look like structured codes.
3. Do not infer a numeric code for sibling products or remove suffixes such as `CA16` and `S53`. Treat the complete string as opaque discovery evidence until a label or official source explains its structure and confirms whether it is generation- or region-specific.
4. Current Mozart devices discovered by Home Assistant's Bang & Olufsen integration use the discovered or selected product model name. An official product-name directory therefore already supports that exact discovery value. A duplicate alias equal to the directory ID is unnecessary, but preserve a genuinely different model string from another integration, including older products exposed through Cast, when evidence shows it is required for discovery.
5. Keep manufacturer values exact. `Bang & Olufsen` is the current Home Assistant integration value. Retain historical or product-brand forms such as `B&O` and `Beoplay` in `manufacturer.json` only when established profile or integration evidence shows they are needed; do not invent punctuation or spelling variants.
6. Because the model is already shown separately, keep `name` concise and descriptive without repeating the directory ID. Prefer a manufacturer-supported product category such as `Wireless Speaker`, `360° Speaker`, `Multiroom Speaker`, or `Bookshelf Speaker`, and avoid subjective slogans such as “powerful” or “premium”. For speaker calibration, investigate unexpected non-monotonic volume curves, but retain a contributor-verified behavior such as device protection reducing output at maximum volume when the original PR documents a repeat measurement and explanation.
When the printed type code, official product name, and integration-reported model cannot be reconciled, keep the established profile available and ask for the product label, exact Home Assistant `manufacturer`, `model`, and `model_id`, the originating integration, and the product generation or variant.

## Bosch

Prefer Bosch's official device type designation as the canonical ID. Preserve exact Home Assistant or Bosch API values such as `SmartHomeController` and `PLUG_COMPACT` in `aliases`. For the Smart Plug Compact profile, use `BSP-EZ` canonically and retain `BSP-FZ` and `PLUG_COMPACT` as aliases because Bosch documents both plug variants together with the same electrical specifications. Use concise display names without repeating `Bosch`.

## Bose

Prefer the numeric type/model designation in Bose's official documentation as the canonical ID. Add a regional product number such as `767520-2100` to `aliases` only if an integration is confirmed to report it. Home Assistant's SoundTouch integration uses `Bose Corporation` as manufacturer and the device API's type value, such as `SoundTouch 10` or `SoundTouch 300`, as model; preserve those exact values as discovery aliases. Use concise names ending in the product type, such as `Wireless Speaker` or `Soundbar`.

## Calex

Prefer Calex's official numeric product code as the canonical ID. Calex product pages and declarations list that code separately from the retail EAN; store a verified barcode in `ean`, not in `aliases`, unless Home Assistant independently reports it as a discovery model. Use specifications from the exact matching product code and do not transfer wattage, lumen output, or barcodes between fittings, shapes, finishes, or revisions.

## Cree

Prefer Cree's official ordering or model code as the canonical ID; do not use an Amazon ASIN as the directory when a verified Cree code exists. Retain an established ASIN as an alias when it remains useful for exact profile lookup. Preserve exact integration-reported values such as the Zigbee model `Connected A-19 60W Equivalent` or an opaque Tuya model string in `aliases`. A Connected Max device may require `Tuya` in `manufacturer.json`; accept that shared manufacturer alias only with the exact device model alias, so PowerCalc can offer all matching candidates without treating unrelated Tuya devices as Cree. Use concise product names without repeating Cree and format actual power and light output compactly, for example `(7.5W, 800 lm)`.

## Dreame / Dreametech

Prefer the exact type code from Dreame's declaration, manual, or product label as the canonical ID; for example, `RLS3D` identifies the D10 Plus. Preserve the exact `dreame.vacuum.*` value exposed by the Dreame Vacuum integration as a discovery alias. Use `Dreame` as the canonical manufacturer name and retain established integration values such as `Dreametech` and `Dreametech™` as manufacturer aliases. Do not infer a type code from a marketing family: X50 Ultra and X50 Ultra Complete each have several robot and base-station combinations, so request both printed labels, the exact variant, and current HA `manufacturer`, `model`, and `model_id` before migrating a profile. For the verified X50 Ultra Complete combination, the robot is `RLX85CE-5`, the base station is `RCXE0109`, and the Dreame Vacuum integration reports `dreame.vacuum.r2538z`; use the robot type as canonical, keep the integration model as an alias, and record the base-station type in the measurement description rather than as a discovery alias.

## Dreo

Use the exact `DR-...` model returned by the Dreo cloud and printed or documented for the device as the canonical ID. The official Dreo HA integration sets manufacturer `Dreo` and model directly from the cloud `model`; its `deviceName` is a device name, not a model alias. Use Dreo's current product mapping for the display name, but do not add marketing names as aliases unless Home Assistant actually reports them as model values. A grouped mapping such as `DR-HTF008S/DBTF08S` establishes a product family, not identical power consumption; add the sibling code as an alias only when official electrical specifications or measurements establish equivalence. For fan profiles, compare calibration plateaus with the integration's supported physical speed count because repeated percentage points within one speed step are expected.

## EGLO

Prefer EGLO's numeric material number as the canonical directory ID and confirm that it identifies the measured variant through an official technical data sheet, the product label, and the original profile evidence. An AwoX value such as `33955` or `33957` is a Zigbee discovery identifier, not necessarily an EGLO article number: the same value may be exposed for lamps with different dimensions, rated power, and consumption. Preserve an exact confirmed AwoX value in `aliases`, including on multiple profiles when necessary, so PowerCalc can offer every matching candidate; never merge profiles or share measurements based on that alias alone. Accept `AwoX` as a manufacturer alias only when Home Assistant Device Info exposes it as the manufacturer. Use the official product family and correct fixture type in `name`, adding a distinguishing dimension where useful, for example `SALOBRENA-Z Ceiling Light (1200x300mm)` or `FUEVA-Z Surface-Mounted Light (285mm)`. Cross-check the LUT peak against EGLO's rated maximum including the driver, and document when multiple identical lights were measured together and the result was divided per device.

For EGLO light metadata, use the official material-number technical sheet and record the complete fixture input power including its driver, not only the replaceable or integrated LED module rating. EGLO's GTIN is often printed on that same sheet; keep it in `ean` and keep the distinct material number canonical.

## Elgato

Prefer the exact `20...` hardware model printed on the device or established by Elgato/Corsair regulatory documentation. Do not substitute a `10...` retail SKU merely because it appears on an official store or MSRP page. A barcode attached to a `10...` retail SKU is usable only when official packaging data and regulatory documentation map that SKU to the exact canonical `20...` hardware model. Preserve the exact product string exposed by Home Assistant, such as `Elgato Key Light`, as a discovery alias when it identifies the measured hardware; exclude per-device name suffixes, serial numbers, and MAC addresses. Keep hardware generations separate—for example, original Key Light `20GAK9901` and Key Light MK.2 `20GAK9902` require distinct evidence and must not share calibration merely because their product names are similar. Use Elgato's model-specific support specifications for lumen output and maximum power, and do not transfer current-generation store-page data to an older `20...` model. Use concise official product wording in `name` and omit `Elgato`.

## Elgin

Prefer the complete alphanumeric code printed on the bulb or packaging, preserving all zeroes and suffixes. Product-label evidence takes precedence when a historical Elgin URL redirects to a different current wattage or variant. Use the label-rated wattage in concise names such as `Smart Color 10W`, `Smart Color 15W`, and `Smart Color 30W`, and omit `Elgin`. Elgin can assign a new EAN to the same unchanged product code; retain both barcodes when official or exact-product evidence maps each one to the canonical code and the specifications still agree. Add an opaque Tuya or Tuya Local model value only when exact HA Device Info, diagnostics, or the original PR maps it to the measured hardware; do not infer sibling values and do not add generic `Tuya` as a manufacturer alias. When HA evidence contains no visible model, ask for the exact `model` and `model_id` plus the originating integration before adding an alias.

## eQ-3 / Homematic

Use the exact official short designation as the canonical ID, preserving the established `HM-...` and `HmIP-...` spelling. Numeric values such as `143166A0` and `150239A0` are product or order references and are not preferable when the short designation is both official and reported by Home Assistant. A casing-only HA value such as `HMIP-PSM` does not need an alias because PowerCalc model matching is case-insensitive. Use the official actuator category in `name` without repeating the short designation. For actuators controlling an installation-specific load, model only credible self-consumption and let the user configure the load; set `only_self_usage` only for the exact devices with an integrated power meter, such as `HM-ES-PMSw1-DR`, `HmIP-BSM`, `HmIP-FSM`, `HmIP-FSM16`, and `HmIP-PSM`. Keep it off non-metering switches and dimmers. Homematic datasheets commonly specify only standby power consumption; use that as a cross-check for measured standby behavior and do not copy it to `device_specs.rated_power`. Leave `rated_power` unset unless eQ-3 publishes a nominal operating figure for the device itself. When Powercalc average mode collected repeated standby readings, use `measure_method: script` and describe the duration, dummy load, and averaging without calling the measurement manual.

## Epson

Prefer Epson's product model designation, such as `ET-3760` or `L6270`, as the canonical directory ID when the official product or support page maps it to the measured printer. Home Assistant's IPP integration can report the same value with a ` Series` suffix; retain the exact confirmed string, such as `ET-3760 Series`, in `aliases` for discovery. A `C11...` value on an Epson page is commonly a regional product or order SKU and is not automatically preferable to the product model or useful as an alias unless the device label or integration proves otherwise. Use the official printer category in `name` without repeating Epson or the canonical ID. Preserve measured IPP state values when the original PR documents them; do not invent soft-off consumption for `unavailable`, because that state cannot distinguish a plugged-in soft-off printer from one disconnected from power. A casing-only `EPSON` manufacturer alias is unnecessary because manufacturer matching is case-insensitive.

## Eufy

Prefer Eufy's short product code, such as `T8400` or `T8410`, as the canonical directory ID when an official product or support page maps it to the measured camera and Home Assistant reports the same value. A longer store code such as `T84001W1` can identify a regional or packaged retail variant; do not add it as an alias unless the product label or integration actually reports it as the device model. Use the current official camera name, such as `Indoor Cam C120` or `Indoor Cam E220`, instead of an older generic capability name. Preserve `Eufy Security` in manufacturer aliases when exact Home Assistant Device Info reports that value. For fixed camera profiles, keep materially different modes such as normal operation and infrared night vision as explicit subprofiles. When Powercalc average mode collected readings for each state, use `measure_method: script` and state the averaging duration clearly.

## Eve

Prefer the `20...` model number from Eve's official accessory-identification page or device label as the canonical ID; Eve lists a separate `10...` product number for ordering. Regional Eve Energy variants have distinct model numbers and plug formats, so share one measured profile through aliases only when measurements or equivalent electrical evidence support it. Preserve an exact numeric Matter product ID such as `80` as a discovery alias when Home Assistant reports it separately from the descriptive model and combines it with the manufacturer; do not infer numeric IDs from the Matter device class. Add `Eve Systems` as a manufacturer alias when that is the exact Home Assistant value. Use a concise product description that omits the manufacturer name, and retain `only_self_usage` for Eve Energy profiles because the device has built-in energy metering.

## Everspring

Prefer Everspring's official base model code, such as `AN158`, when current Z-Wave JS configuration and Home Assistant's Z-Wave JS Device Info report the same label. Treat suffixed codes such as `AN1582` and `AN1586` as regional plug variants; do not add them as aliases without exact product-label or discovery evidence and confirmation that the measured hardware applies. For a plug with built-in power metering, retain `only_self_usage: true` so the profile represents the plug's own relay-off and relay-on consumption rather than the attached load. When a very low standby reading is conservatively adjusted because of meter resolution, preserve the measured value and explain the adjustment directly without relying on a stale discussion link.

## eWeLight / eWeLink

Do not treat `eWeLight` and `eWeLink` as spelling aliases or merge their manufacturer directories. They are exact discovery/vendor values with different product provenance, even though the same generic bulb controller may expose either Zigbee manufacturer string. For example, current Zigbee2MQTT fingerprints group `ZB-CL01` devices reporting `eWeLight` or `eWeLink` under a Lonsonho `ZB-RGBCW` definition. That grouping identifies a controller family, not one electrically identical bulb, so neither `ZB-CL01` nor `ZB-RGBCW` is a safe alias for a measured 5W GU10 profile. Conversely, a product-specific device record that maps a complete Zigbee signature to an eWeLink model such as `EZ-E14` is sufficient to use that code canonically while retaining the full signature and historical short value as aliases.

Prefer a specific printed product code or exact integration model such as `SA-003-Zigbee`. An FCC ID such as `2APYC-JL01` may remain as an established manual library ID when no better identifier exists, but do not treat it as a unique model: its regulatory test report covers multiple `JL-*` lamp variants. Keep such a profile manual-only unless an exact, product-specific discovery value is established, and use the verified fitting, power rating, and capabilities in `name`. When a measurement recorder averages readings automatically, use `measure_method: script` and state the averaging duration.

## Fibaro

Prefer the official FIBARO model family code and the exact Z-Wave JS label, such as `FGWP-102`, over a punctuation-stripped directory ID. Keep `Fibargroup` as a manufacturer alias when Z-Wave JS reports it. When migrating an established value such as `FGWP102`, retain it in both `legacy_ids` and `aliases` if it was historically used for profile selection or discovery. Do not infer regional suffixed product codes from the base family unless the product label or Z-Wave JS configuration confirms the variant and the measurements apply to it.

For a Wall Plug with built-in power metering, use `only_self_usage: true`. Its own consumption may be included in Z-Wave meter reports when the device is configured accordingly; document that setup and retain the explicitly reported relay-off and relay-on values. Cite the exact measurement contribution rather than a discussion entry that says the device was not measured.

## Free

When no printed hardware code is established, retain the exact Freebox API and Home Assistant model as the canonical directory, for example `Freebox v8 (r1)`, and keep its separate API `model_id`, such as `fbxgw8-r1`, as an alias. Do not add the Home Assistant UI rendering `Freebox v8 (r1) (fbxgw8-r1)` as another alias: the text between parentheses is the separately matched model ID, not part of the model value. Keep exact manufacturer variants such as `Freebox SAS` and `Freebox` when different integrations report them.

Use Free's official product-role spelling in `name`, such as `Server Pop` and `Player Pop`, rather than repeating a technical revision or retaining all-uppercase `POP`. A historical integration value such as `Freebox Player v8` may remain as an alias when the original contribution confirms it. Do not assume the server and player share measurements merely because both belong to the Freebox Pop offer.

Record both Wi-Fi and Ethernet for Player Pop, which supports either network connection. The established `Freebox v8 (r1)` Server Pop profile uses Wi-Fi. Keep each endpoint's connectivity separate from the paired device's interfaces and hardware revision.

## GE

Prefer the official printed GE Link product code, such as `PSB30-SW27` or `PSB38-BW30`, as the canonical model ID. Use a concise name that distinguishes the bulb shape and capability, such as `Link BR30 Soft White Bulb`.

Legacy GE Link bulbs connected through ZHA may report manufacturer `GE_Appliances` and the generic model `ZLL Light`. Keep `GE_Appliances` as an exact manufacturer alias. Add `ZLL Light` only to profiles for which exact Device Info evidence exists; it may intentionally be shared by electrically different bulbs so PowerCalc can offer the confirmed candidates. Do not promote it to a canonical ID or assume one bulb's measurements apply to other GE Link shapes.

## Genio

Use the exact Mirabella Genio item number printed on the product or confirmed by an official product page, such as `I002741` or `I004544`, as the canonical model ID. A multipack item number remains suitable when the same code is printed on each measured device. Keep the display name concise but preserve distinctions such as cap type, bulb shape, wattage, and RGB+CCT support.

Genio Wi-Fi devices may be exposed by Home Assistant's Tuya integration as manufacturer `Tuya` with a generic model value. Do not add `Tuya` as a manufacturer alias or a `Tuya generic (...)` value as a model alias without evidence that the combined discovery values safely identify the measured product. Verify that every Home Assistant color mode has a corresponding LUT; never infer missing RGB or white-mode measurements from a sibling item number.

Mirabella can reuse an item number for a later hardware revision with different light output. For a measured profile, retain the specifications documented by its original contribution or exact packaging rather than replacing them with values from a current product page solely because the item number matches.

For older monochrome GLS bulbs, match specifications using the complete combination of item number evidence, cap type, fixed color temperature, wattage, and product description. Do not reject an otherwise exact 800 lm listing merely because Genio also sold RGBW GLS bulbs at 800 lm; conversely, do not transfer RGBW metadata to a monochrome profile based on light output alone.

## Gledopto

Use the exact `GL-*` code from the product label, official Gledopto documentation, and Zigbee Device Info as the canonical model ID. Preserve generation and type suffixes such as `P`, `Z`, and `S`; do not normalize them away. A case-only manufacturer difference such as `Gledopto` versus `GLEDOPTO` does not require an alias because manufacturer matching is case-insensitive.

Use a concise display name that distinguishes capability, wattage, fitting or shape, and product category where known. Keep compact wattage notation such as `7W`.

Confirm the reported color modes and hardware or firmware revision before reusing measurements. Gledopto has reused at least `GL-S-007Z` across revisions with different reported capabilities. Do not assume one LUT represents every revision sharing that model code, but do not split a profile or invent an alias unless a distinct stable identifier is available.

For light metadata, accept a barcode only when the listing identifies the complete exact `GL-*` code, including a finish or revision suffix where one exists. A manufacturer lumen range must remain a range in the evidence; do not select one endpoint as an exact `lumens` value.

## Google

Use Google's official printed or regulatory hardware model as the canonical ID, for example `H0ME` for Google Home, `H0A` for Home Mini, `H2C` for Nest Mini, `H2A` for Nest Hub Max, `GUIK2` for Nest Hub (2nd gen), `GXCA6` for Nest Audio, and `H2E` for Nest Wifi Point. Preserve the former directory in `legacy_ids` when migrating a marketing-name directory or correcting a transcription such as `HOA` to `H0A`.

Preserve established PowerCalc discovery models such as `Home`, `Home Mini`, `Nest Mini`, `Home Hub Max`, `Nest Hub 2gen`, `Nest Audio`, and `Nest Wifi Point` as aliases. Do not add a `Google ` prefix merely because the manufacturer or a user-facing product label includes it. PowerCalc searches `model` and `model_id` separately, so when a former directory combines both as `<model> (<model_id>)`, keep only the model portion as the alias and rely on the canonical directory for the model ID; for example, use alias `Home Mini` for canonical `H0A`. Keep the full former directory, including parentheses or a transcription such as `HOA`, in `legacy_ids` for migration only. Keep `Google Inc.` as a manufacturer alias when reported.

For speaker profiles, distinguish idle `standby_power` from the volume-0 value while playback remains active. Record the playback signal and whether volume changes and averaging were manual or controlled by the measurement utility. For a combined speaker and mesh device such as Nest Wifi Point, prefer long-term standby evidence and document that network traffic can change consumption.

## Gosund

Use the exact printed product code with manufacturer capitalization, for example `SL2` for the 5 m RGB LED strip. PowerCalc model matching is case-insensitive, so a reported `sl2` value does not need a duplicate alias; when migrating an existing lowercase directory, retain that former directory in `legacy_ids`. Do not infer a Tuya model alias or `Tuya` manufacturer alias merely because Gosund devices use that platform—require exact Home Assistant Device Info or diagnostics. Gosund hardware revisions can change controller or flashing behavior while retaining a product code, so compare the physical light capabilities and measured modes before sharing a LUT. A slow metering update interval can legitimately make a complete HS measurement take many hours; judge credibility from the completed data and documented update frequency rather than duration alone.

## Govee

Use Govee's exact `H...` product model as the canonical directory ID and confirm it on the official product page or developer model catalog plus Home Assistant Device Info. Govee to MQTT commonly reports that same code as `model`. Matter can instead show manufacturer `Shenzhen Qianyan Technology` and a numeric `model_id` alongside the `H...` model; keep the manufacturer alias, but do not add the numeric ID as a model alias when the canonical code already matches separately.

Use the official product description without the `Govee` manufacturer prefix. Preserve electrically relevant distinctions such as fitting, size, length, panel count, and RGBIC/RGBWW capability; related codes can represent different lengths or pack configurations and must not share a profile merely because the marketing name is the same. Retain a `brightness` LUT only when the originating integration actually exposed `brightness` alongside color modes, even though that is an invalid Home Assistant color-mode combination, because existing installations can still select it.

Govee retail suffixes can encode region, finish, or pack configuration around the same base `H...` hardware model. A single- or multipack barcode may be stored only after the complete SKU is mapped back to the exact base model and every item in the pack is that same device. Prefer regulated product sheets and model-specific manuals for light output and rated power; an adapter's maximum output is not the fixture's rated consumption. An EPREL value for an internal strip section may be scaled to the complete fixture only when the exact sold length is an integer count of identical documented sections and the resulting total is consistent with the measured LUT.

## GreenWave

Prefer the exact printed regional PowerNode model when the measured hardware is known, but retain `GWPN1` as canonical for an established profile whose label variant cannot be recovered. Z-Wave device databases use `GWPN1` as a family/discovery identifier for the one-port PowerNode and document compatibility with both `NS210` and `NS310`; that compatibility does not prove identical self-consumption. Do not add those printed codes as aliases without identifying the measured variant. Preserve `GreenWave Reality Inc.` as a manufacturer alias, while a casing-only `Greenwave` value needs no separate alias. For fixed smart-plug profiles, confirm both off-state and relay-on self-usage and keep `only_self_usage` enabled when the profile represents the plug's own consumption rather than its attached load.

## Hampton Bay

Use the exact model from the product label, official Home Depot product data or Hampton Bay guide and Home Assistant Device Info. Preserve distinctions that affect consumption, including string length, bulb count and RGBW capability, in the display name. `Hubspace` is the control platform and an official product-name qualifier, not automatically a manufacturer alias; likewise, do not add an underlying supplier such as Novolink unless Home Assistant reports it. Treat finish suffixes and longer sibling strings as separate models until official specifications and measurements establish electrical equivalence. Home Depot's unqualified `Lumens` field for these string-light products describes the complete string: verify this by comparing otherwise equivalent bulb-count/length variants rather than multiplying the value by the bulb count. Keep a discovery remark when dynamic effects cannot be represented by the available LUT strategy.

## Harman Kardon

Home Assistant's Cast integration exposes the Cast `model_name` and does not provide a separate `model_id`. Retain an established exact Cast value as the profile ID when the measured unit's printed article code is unavailable. Keep the known generation in the profile ID and display name when the measurement identifies it, but preserve a confirmed generation-less Cast value such as `HK Citation One` as a shared discovery alias. Do not promote a regional, finish-specific retail SKU such as `HKCITAONEMKIIBLKAS` or `HKCITAMB700BLKEU` without exact label evidence tying it to the measured unit.

For speaker calibration, use a stable audio source and averaging for volume steps. Model playing-volume power separately from idle standby and delayed network sleep where the device supports it, and compare the sleep delay and consumption with official technical documentation. Keep names concise and omit `Harman Kardon` or an `HK` prefix when the model is already stored separately.

## Hornbach / FLAIR Viyu

Prefer Hornbach's exact printed article number as the canonical profile ID when its official product page, instructions or compatibility list maps it uniquely to the measured device. Preserve the complete Zigbee model reported by Home Assistant, such as `VIYU_A60_806_CCT_10454469`, as an alias. Use `Hornbach` as the library manufacturer and retain the exact discovery value `HORNBACH Baumarkt AG` as a manufacturer alias; `FLAIR Viyu` is the product brand and family, not automatically another discovery manufacturer.

Use a readable product name with official brand casing and concise electrical distinctions, for example `FLAIR Viyu A60 E27 CCT Bulb (8.5W, 806 lm)`. For low-power lamps measured in parallel because one lamp falls below the meter's resolution, verify that the recorder divided every reading per lamp, document the count and resolution limitation, and compare standby and peak power with official specifications.

## HP

Prefer the exact product model printed on the device when it is known. Otherwise retain the established IPP model value as canonical, for example `MFP 3301`, and preserve the full IPP value `HP Color LaserJet Pro MFP 3301` as an alias. Home Assistant's IPP integration maps the printer's IPP model directly to `model`, exposes the serial number separately and does not set `model_id`; do not turn a serial number or a bracketed UI suffix into an alias.

HP's `3301` family includes more specific suffix variants such as `3301fdn`, `3301sdw`, `3301fdw` and `3301cdw`. Do not guess one from a generic IPP value or add every family member as an alias. HP regulatory model numbers such as `SHNGC-2201-01` are explicitly separate from product model numbers and are not preferable discovery identifiers. Use a descriptive product category such as `Color LaserJet Pro Multifunction Printer` for `name` instead of repeating `MFP 3301`. For IPP fixed profiles, document that the `idle` state may cover several physical power phases and that polling can miss short warm-up, fusing and cool-down peaks; preserve measured idle power without pretending that it models per-page energy. The established `MFP 3301` profile uses Wi-Fi; do not infer Ethernet merely because IPP can operate over either network transport.

## IKEA

Prefer the exact IKEA **Model identifier** or printed type designation for the measured regional product. Do not replace a confirmed code with a wildcard assembled from regional siblings: for example, the Schuko TRETAKT measured in the original contribution is type `E2204`, while `E2214` is a separate 10 A regional variant. A family-like value such as `E22x4` is suitable only as a historical `legacy_ids` entry after migration, not as a canonical model or discovery alias.

For light metadata, prefer an IKEA product page whose **Technical information** names the exact model identifier; it can provide rated power and luminous flux even when the retail description is shared by several revisions. An official IKEA product guide is also suitable when it explicitly pairs the type designation with the specification. IKEA article numbers such as `404.867.83` identify retail articles but are not EAN barcodes, even when the packaging prints them below a barcode; do not copy, checksum-transform, or pad them into `ean`. Add a separate GS1 code only when exact packaging evidence also confirms model, article, region, fitting, and revision, including for multipacks.

Keep the exact integration-reported description and model ID as separate discovery values. Home Assistant can render these together as `<model> (<model_id>)`, but the parentheses are presentation and PowerCalc matches the fields separately. For example, use aliases `TRADFRI LED driver, 30 w` and `ICPSHC24-30EU-IL-1/ICPSHC24-10EU-IL-2`, not one concatenated or malformed UI string. Preserve an exact descriptive alias on every confirmed profile when integrations reuse it across regional variants, such as the INSPELNING plug models; PowerCalc can offer the matching candidates, but do not infer that the variants share measurements.

Use a concise official product description for `name`, without repeating the canonical code or adding the transport protocol. Retain meaningful distinctions such as fitting, colour capability, dimensions and regional lumen output. Use compact wattage notation such as `30W`, but preserve exact integration spelling such as `30 w` in an alias when needed for discovery. When old product-name directories were migrated to official codes, add those released directory values to `legacy_ids` even if the same string already remains in `aliases` for discovery.

IKEA's numeric Matter product IDs may be retained as exact discovery aliases when Home Assistant reports them and the CSA certification maps them to the product. Verify each ID independently rather than assuming global uniqueness. For LUT profiles, scan the complete manufacturer for quality outliers: low-brightness colour-temperature transitions can capture unsettled readings across an entire curve, so correct only scanner-confirmed isolated points and regenerate the affected plots. Respect profiles in the validator's manually verified skip list; a profile documented there as messy or needing remeasurement must not be auto-smoothed into apparent validity.

## Inovelli

Use the exact Inovelli SKU as the canonical profile ID, including protocol and feature suffixes such as `VZM31-SN` and `VZM32-SN`. Confirm it in the official device manual and the raw Zigbee signature. Use the official series/product description in `name`, such as `Blue Series Dimmer Switch`, without repeating the canonical SKU. When the device already meters the connected load but omits its own consumption, retain a fixed profile with `only_self_usage: true`; do not convert that device-only measurement into a load-estimation curve.

## Intelbras

Preserve the spacing of Intelbras' printed model code, for example `EWS 410`; retain an old compact library ID such as `EWS410` only in `legacy_ids` after migration. A Tuya-based integration can expose generic manufacturer or device information, so do not add `Tuya` or a user-facing Tuya Local name as a discovery alias. Intelbras may group a discontinued and successor product on one current page; do not treat the successor code as an alias or share measurements without exact hardware evidence. Use compact wattage notation in the display name.

## JBL

Prefer the product number from an official JBL declaration or device label. JBL may publish regional, color-specific item numbers such as `JBLLINKMUSICBLKEU`; do not promote one of those SKUs without evidence that it identifies the measured variant and is useful for discovery. Home Assistant's Cast integration exposes the Cast model and manufacturer without a separate model ID, so preserve exact confirmed values such as `JBL Link Music` and `HARMAN International Industries` as discovery aliases. When an established directory differs from the official product number only in casing, avoid a migration that adds no discovery or compatibility benefit. Use the official product category, such as `Wi-Fi Speaker`, for `name` when the product number would otherwise be repeated.

## Kauf

Prefer the model printed on the bulb housing, such as `BLF10`, as the canonical profile ID. Kauf's ESPHome firmware uses a generic RGBWW project/model value across bulb shapes, sizes, wattages and flash variants, so do not add `RGBWW` as an alias or use it to infer electrical equivalence. A parenthesized `(4m)` after a printed model denotes flash size; do not create a separate power profile or alias for it unless measurements establish different electrical behavior. Use a concise bulb description in `name`, including the shape and compact wattage where useful.

## Kogan

Use the complete Kogan product code as canonical, preserving fitting, wattage and pack distinctions. For example, `KASB10WB22HA` and `KASB10WE27HA` are separate B22 and E27 products covered by the same manual, not aliases for one another. Add an integration-reported alias only when exact Home Assistant Device Info or diagnostics maps it to the measured product; use of Tuya Local alone does not justify generic `Tuya` manufacturer or model aliases. Keep the fitting and compact wattage in the display name when they distinguish the variant.

## LEDVANCE / OSRAM

Apply these rules when reviewing or normalizing LEDVANCE and OSRAM profiles:

1. Prefer the exact official manufacturer article/model code as the directory ID. LEDVANCE codes such as `AC26447` take precedence over a GTIN, marketing name, integration-specific value, or malformed concatenation when official product data establishes the mapping. Older OSRAM article numbers and device-reported Zigbee model strings remain valid canonical IDs when they are the most specific verified manufacturer identifier available.
2. Confirm the mapping with an official LEDVANCE product page, datasheet, declaration of conformity, catalog, or product label. Use Home Assistant diagnostics, the integration's raw device information, LEDVANCE firmware data, or an established Zigbee catalog to determine which identifiers are actually reported for discovery.
3. Add exact alternative identifiers for the same hardware to `aliases`. These may include another integration's stable model string, an official Zigbee model string, or a GTIN/EAN that official data maps one-to-one to the article. Do not add a transcribed typo or an unverified numeric string as an alias.
4. Keep a malformed former value or purely internal slug out of `aliases` unless an integration is confirmed to report it.
5. Do not create separate profiles solely for a housing color or finish when official specifications show the variants are otherwise the same device and the measurements apply to both. Prefer the measured article code when known; otherwise choose one documented article code as canonical and add the other cosmetic article codes and their exact GTINs as aliases. Keep color out of `name` when the profile intentionally covers every finish.
6. Do not combine variants that differ in wattage, fitting, dimensions, light capabilities, electronics, or measured behavior. A shared marketing family or Zigbee model string alone is insufficient evidence that these variants can share measurements.
7. Keep display names consistent with the established LEDVANCE spelling: `SMART+` and `LIGHTIFY` for product families, and `WiFi`, `ZB`, `RGBW`, `RGB + TW`, `TW`, and `DIM` where those terms describe the verified product. Put the complete product description in `name`, omit the manufacturer name itself, and do not include a color/finish that is intentionally represented by aliases. Use compact ratings and dimensions, such as `9W` and `600x600mm`.
8. Preserve OSRAM's official all-caps styling in `manufacturer.json`. Current verified OSRAM Zigbee mappings include `Classic A60 RGBW` to `AA69697`, `CLA60 TW OSRAM` to `AC03642`, `PAR16 50 TW` to `AA68199`, `Classic B40 TW - LIGHTIFY` to `AB32840`, and `Plug 01` to `AB3257001NJ`. Use the code as canonical and the exact reported model string as an alias; retain a replaced released directory ID in `legacy_ids`.
9. When a raw OSRAM Zigbee model string and its mapped article code exist as duplicate profiles, consolidate them only after the converter mapping establishes they are the same hardware. Prefer the LUT with the better-documented measurement and credible standby reading, preserve the discarded profile's exact discovery value and legacy ID, and retain contributor attribution. If the supposedly identical LUTs differ materially from each other or from the official rating, first resolve the measured revision and entity semantics instead of choosing a canonical LUT from meter reputation alone.

When the official code and the measured variant still cannot be reconciled, ask the contributor for a product-label or packaging photo, the exact identifier from Home Assistant or the originating integration, and confirmation of relevant capabilities such as RGB versus tunable white. Do not block on a cosmetic finish alone when the variants are demonstrably electrically identical.

## Ledworks / Twinkly

Prefer the exact Twinkly base product SKU exposed by Home Assistant, such as `TWS600STP`. Preserve the capability suffix: `STP` identifies the Multicolor/RGB Strings while `SPP` identifies Multicolor + White and must remain a separate profile. Do not add plug-, wire-color-, or region-specific suffixes such as `-BEU` unless Home Assistant reports that complete value. A regional packaging EAN is safe only when the original profile evidence identifies the exact regional suffix and compatible power supply; keep the base SKU canonical and do not add sibling-region barcodes. Use Twinkly's product-family wording in `name`, including the LED count and color capability where they distinguish variants. A fixed profile can be credible for an effect light when extended measurement shows that effects and brightness settings produce approximately the same input draw; retain that evidence in `measure_description` rather than assuming a LUT is required. Do not treat an adapter output rating alone as complete-device `rated_power`; accept the same value when Twinkly independently lists it as product `Power` or `Power rating ... MAX`, especially when the measured on-state draw corroborates it.

## Lenovo

Prefer Lenovo's generation-specific hardware model as canonical: `CD-24501F` identifies the first Smart Clock, while `CD-24502F` is Smart Clock 2. Preserve the exact Cast product name, such as `Lenovo Smart Clock`, as a discovery alias when confirmed, but do not infer that later generations share it or their calibration. Use the concise product name without repeating Lenovo. For speaker calibration, retain the measured volume points even when the curve has a step caused by amplifier behavior; never invent a missing muted or 0% point from idle standby power.

## Lexman

Prefer the exact model identifier from ADEO's product-information sheet when it matches the Zigbee model, such as `IG-CDZOTAAG014RA-MAN`. Keep `ADEO` as a manufacturer alias when integrations report the supplier instead of the Lexman brand. Treat an eight-digit Leroy Merlin reference such as `84372273` as a retailer article number, not a model alias, unless Home Assistant is confirmed to expose it. Use a concise product name without repeating Lexman or the canonical model ID, and write wattages without a space, for example `14W`.

## Innr

Apply these rules when reviewing or normalizing Innr profiles:

1. Use the exact Innr type code as the canonical directory ID, including spaces and capability suffixes: for example, `RB 285 C`, `RS 230 C`, and `SP 240`. Do not collapse the spaces, remove suffix letters such as `C` or `T`, or replace the code with a marketing-family name.
2. Confirm the code through the type-number section of an official Innr product page, a declaration of conformity, or the product label/manual. Use Home Assistant device information and the raw Zigbee signature to establish what integrations actually report. Treat Zigbee2MQTT mappings as corroboration rather than proof that grouped devices have identical power behavior.
3. Add a stable integration-reported model string to `aliases` only when it uniquely maps to the same hardware. A verified value such as `On/Off plug (SP 220)` is a valid alias; do not generate aliases merely by removing spaces or punctuation from the canonical code.
4. Do not add retail bundle suffixes such as `-2` or `-4`, EANs, or package SKUs as aliases unless an integration demonstrably reports them as the device model. Likewise, do not alias regional siblings such as `SP 240`, `SP 242`, and `SP 244` without evidence that socket type, voltage range, electronics, and measured self-usage are equivalent.
5. Keep distinct type codes separate when they merely share a marketing name or appear in one Zigbee catalog entry. For example, sibling codes such as `RB 247 T`, `RB 248 T`, and `RB 249 T` are not interchangeable without official hardware equivalence and compatible measurements.
6. Put the official product description in `name`, omit the manufacturer and protocol name (`Innr`, `Zigbee`), and match the verified fitting and capabilities. Prefer concise Innr family wording such as `Smart Bulb Colour E27`, `Smart Plug`, `Strip Light Colour`, `Outdoor Smart Plug`, and `Round Ceiling Light`. Do not claim tunable-white or colour support when the profile strategy and device capabilities do not support it.
7. Cross-check `only_self_usage` per exact plug model. Current verified mappings are metering for `SP 120` and `SP 240`, and non-metering for `SP 220`, `SP 224`, and `OSP 210`. Reverify when a hardware revision or regional model differs; do not transfer the flag based only on the shared `SP` or plug family name.
8. Innr can reuse a type code and even its retail EAN across electrical revisions. Compare the source date and specifications with the LUT peak before filling metadata. When an EAN cannot distinguish the measured revision from a materially different current revision, omit it even though it is valid for the product family. Never transfer metadata among `RB 247 T`, `RB 248 T`, and `RB 249 T`; they have distinct power and lumen ratings.
9. Treat an internally inconsistent reseller feed as weaker than contemporaneous product documentation and multiple exact-EAN records. For example, `RF 263-2 /05` is described as 350 lm on the same page whose generated specification table says 390 lm; the original Innr presentation and exact single- and two-pack EAN records consistently establish the measured `RF 263-2` as 4.2W/350 lm. Keep pack suffixes out of discovery aliases even when they help identify the metadata revision.
10. When several profiles measured by the same contributor, meter and tool version all overread their exact historical product ratings, do not invent parallel hardware revisions solely to make each LUT peak fit. Exact model-and-EAN records can still establish `device_specs`; track the shared measurement-quality concern separately and preserve the measured LUT until it can be remeasured.

When an Innr code, integration-reported value, or regional variant cannot be reconciled, ask for the product-label type code, the exact Home Assistant model value and originating integration, and confirmation of fitting, region, and built-in power metering before adding aliases or sharing measurements.

## Lidl

Apply these rules when reviewing or normalizing Lidl profiles, including Livarno and Silvercrest products:

1. Prefer the exact code printed on the product or packaging as the canonical directory ID. Lidl lighting commonly uses an `HG...` type code with a meaningful variant suffix; some products instead have a stable article code or an IAN-based identifier. Preserve punctuation and suffix letters that distinguish the measured hardware.
2. Reject generic Tuya/Zigbee model values such as `TS0502A` and `TS0505B` as directory IDs and aliases unless one-to-one uniqueness for the measured hardware is demonstrated. If the integration exposes only such a shared value, keep the profile manual-only rather than enabling unsafe automatic discovery.
3. When the directory code conflicts with the model recorded in an older PR, Zigbee catalog, or Home Assistant Device Info, do not infer equivalence from matching capabilities or an A/B/C suffix. Ask for the printed product code and the current exact Home Assistant `manufacturer`, `model`, and `model_id`, including the originating integration and values after any linked converter fix.
4. Zigbee2MQTT can expose one Home Assistant `model_id` that explicitly lists several printed Lidl codes, separated by slashes. PowerCalc already splits such reported values and offers the canonical profiles for the listed codes as separate candidates. Do not add the compound value or another listed code as an alias solely because they share this discovery value, and do not infer electrical equivalence from the grouping. A listed code without its own measured profile remains unsupported. A stale PR value, converter bug, or catalog mapping that was never exposed by Home Assistant is not a discovery alias.
5. Treat raw Zigbee manufacturer strings such as `_TZ...` as signature evidence, not automatically as `manufacturer.json` aliases. Confirm that ZHA, Zigbee2MQTT, or another integration exposes the exact value as the Home Assistant device manufacturer. Remove exact duplicate aliases, but request current Device Info evidence before deleting a distinct historical alias that may still support discovery.
6. Keep hardware variants separate when their printed codes differ unless official documentation and measurements establish electrical equivalence. Codes ending in `A`, `B`, or `C` may represent distinct fittings, shapes, electronics, or capabilities even when a Zigbee catalog groups them.
7. Use the verified product description in `name`. Lidl's product brands such as `Livarno Home`, `Livarno Lux`, and `Silvercrest` may remain because they identify the branded product even though the library manufacturer is Lidl. Normalize capitalization and capability terms, but do not invent details absent from the label, official documentation, or device capabilities.
8. Use the complete luminaire or kit input power for `rated_power`, including its supplied driver or power supply, rather than the LED module rating or adapter capacity in isolation. Exact A/B/C manual tables are suitable for metadata, but never copy one suffix's values to a sibling whose own row or manual is absent.
9. An EPREL entry can describe one internal LED module rather than the complete multi-module fixture. Compare dimensions, module count, manual totals, and the measured profile before using its lumen or power values. Multiply module flux only when exact documentation establishes the number of identical modules and their dimensions prove that the EPREL source is internal; otherwise do not manufacture a fixture total.
10. A Lidl document may list several consumer barcodes for an article family without mapping each barcode to its `A`, `B`, or `C` type-code suffix. Do not assign those barcodes by order or fitting inference; require an exact label, packaging record, or product listing that shows both the suffix and barcode.

When a Lidl label code and integration-reported value cannot be reconciled, ask for a product or packaging label photo and the current Home Assistant Device Info fields. Keep the profile discoverable only through identifiers proven specific to the measured variant.

## Lightinginside

Prefer the product-specific code printed on the bulb or packaging as canonical, excluding pack-count suffixes unless they identify the individual device. Home Assistant's Matter integration can expose manufacturer `LIGHTINGINSIDE by Launray`, model `Smart LED Bulb`, and the numeric model ID `1718` for multiple electrically different bulbs. Preserve the exact manufacturer value and `1718` as shared discovery aliases on confirmed profiles so PowerCalc can offer every matching candidate, but never merge profiles or share measurements from that ID alone. Make the display names distinguish those candidates with verified characteristics such as `9W` versus `11W`.

## Lindby

Prefer Lindby's exact model identifier from the product label or EPREL registration as canonical. A Tuya-based Lindby bulb can appear in Home Assistant as manufacturer `Tuya` and a generic model such as `bulb RGBW`; this combination is not specific enough for a model alias or automatic profile discovery. Retain an established exact manufacturer alias when the original Device Info confirms it, but do not add the generic model value. Use a concise name that reflects measured capabilities such as RGBW, fitting, and compact wattage without repeating the canonical identifier. The established `9971013` bulb is Wi-Fi; do not generalize that transport to other Lindby/Tuya products.

## Linkind

Prefer the exact code printed on the bulb or packaging as canonical. For older Zigbee products, a reliable device mapping may establish a product-specific Linkind code such as `ZL1000701-27-EU-V1A02`; keep the raw model such as `ZBT-DIMLight-D0120` as a discovery alias when it maps exactly to the measured product. Matter may report manufacturer `Leedarson` and numeric product IDs such as `1011` or `4120`; preserve those exact manufacturer and model aliases when the original Device Info confirms them. Retain historical manufacturer alias `lk` when the originating discovery evidence supports it. Regional single- and multipack barcodes are usable only when the exact `LS...` product code, fitting, wattage, and light output match. When exact sources disagree on a rating for the same `LS...` code, treat it as a possible hardware revision: retain the value supported by the measured LUT and request the measured label or date code before changing it. Use concise English names with the fitting or shape, colour capability, compact wattage, and a spaced lumen unit; omit the manufacturer name and redundant terms such as `Smart` and `Wifi`.

## LSC

LSC Smart Connect products can have a consumer-facing Action product number, an LSC/Electro Cirkel article or EPREL model identifier such as `970710`, and one or more factory model strings. Prefer the exact LSC/Electro Cirkel or EPREL identifier when authoritative documentation maps it to the measured hardware. Retain an established specific Action article code when no more authoritative code is verified; do not guess a `970...` replacement. An official Electro Cirkel declaration that pairs the exact article code with a GTIN is strong barcode evidence; generic Action listing numbers are not. Keep an exact factory or Tuya model string as an alias only when Home Assistant Device Info or the original contribution confirms that discovery value. Do not add generic manufacturer `Tuya` merely because LSC uses the platform; it is acceptable only as an intentional compatibility alias after moving an existing `Tuya/<exact model>` profile and verifying that the complete pair resolves uniquely. Preserve revision suffixes such as `.1` only when the measured label establishes them. Use concise names without repeating LSC, `Smart`, or `WiFi`, and format ratings compactly, for example `9W` and `806 lm`. The established `3012586` ceiling light uses Wi-Fi; do not infer connectivity for sibling article numbers.

## Luedd

Prefer Luedd's five-digit product/model number as the canonical ID when its official product page or product-information sheet maps it to the measured lamp. When migrating an established descriptive directory, retain that exact value in both `aliases` and `legacy_ids` so discovery and saved selections continue to work. A Tuya manufacturer alias is acceptable only when the original Home Assistant Device Info confirms it and an exact model value limits the match to the measured product; never add generic Tuya model values by inference. Use a concise product description in `name` without repeating Luedd or the canonical number. Compare measured standby with the official networked-standby value and low-brightness LUT readings; ask for remeasurement when they materially conflict rather than silently substituting nominal data.

## Lumiman

Prefer the printed Lumiman model code as canonical. Treat a base code such as `LM530` as a product family when evidence shows separate E26, E27, or B22 variants; identify the measured fitting and region in `name`, and do not add sibling variant codes as aliases without exact discovery evidence and electrical equivalence. A profile contributed for manual lookup because LocalTuya reports generic manufacturer `Tuya` and model `LUMIMAN SMART BULB` must remain manual-only: those values do not uniquely identify Lumiman hardware and are unsafe aliases. Use concise capability, shape, fitting, wattage, and lumen details in the display name.

## Lutron

Prefer Lutron's exact complete model number, including product and color or regional suffixes such as `PD-3PCL-WH`, when the official product page and Home Assistant agree. The Lutron Caséta integration constructs Device Info model text as `<model> (<type>)`; PowerCalc matches the parenthesized part separately, so preserve a confirmed type such as `PlugInDimmer` as an alias without adding the combined UI rendering. Keep the exact integration manufacturer `Lutron Electronics Co., Inc` as an alias. For a non-metering dimmer, omit `linear_config` and record only its credible off-state and relay-on no-load consumption; do not set `only_self_usage`. Use Lutron's concise product category in `name` without repeating marketing synonyms.

## Malmbergs

Prefer the exact Malmbergs article number as the canonical profile ID when the official product page maps it to the measured device. For example, article `9970020` identifies the 36W Sirius III WiFi Tune LED panel. Use concise product-family wording in `name` without repeating Malmbergs or the article number. Do not add that marketing name or an opaque WiFi-platform value to `aliases` without exact Home Assistant Device Info or originating-integration evidence. Cross-check a light LUT's peak against the official rated input; the existing `9970020` calibration peaks close to its 36W rating.

## Mammotion

Use the exact Mammotion product model as the canonical ID when the manufacturer and Home Assistant agree, preserving official capitalization and spacing such as `YUKA mini 600`. Home Assistant can display identical `model` and `model_id` fields as `YUKA mini 600 (YUKA mini 600)`; PowerCalc matches those fields separately, so neither the combined presentation string nor a duplicate alias is needed. Keep `name` descriptive without repeating the model, for example `Robot Lawn Mower`.

For grid-side mower profiles, distinguish the mower's operating state from charging-station consumption. The Mammotion integration's lawn-mower state does not reliably imply active charging, while its `battery_charging` binary sensor does. Prefer that device-class sensor for the charging branch and use measured charging cycles to verify tapering near full battery; model non-charging mower states as the measured idle draw of the powered charging setup.

## MeLiTec / LightZone

Use the exact model printed prominently on the type plate as the canonical ID, preserving MeLiTec's official capitalization. For the LightZone pendant light, that is `DP15`; the same label lists design reference `8220/01/11`, article `2839`, and EAN `4047247308793` separately, so do not treat those as model aliases without exact Home Assistant evidence. Retain `LightZone` as the established manufacturer alias for the ESPHome-converted device. Use an English descriptive `name` without repeating the brand or canonical ID, and compact wattage notation when the label distinguishes the product, for example `LED Pendant Light (19W)`. A measured LUT maximum moderately below the approximate type-plate input remains plausible; investigate large curve deviations independently rather than forcing the peak to the rating.

## Mercator Ikuü

Use the official Mercator/Ikuü SKU as the canonical profile ID, preserving its complete suffixes, for example `SMD4109W-RGB-ZB`. When an original measured product page calls a downlight 92 mm but a current listing rounds the cut-out to 90 mm, retain the measurement-era dimension unless evidence shows that the hardware itself changed. A shared Tuya model such as `TS0505B` may be added as a model alias only when the profile also contains the exact product-specific manufacturer values demonstrated by Home Assistant or current converter fingerprints. Preserve both a historical value such as `_TZ3210_xr5m6kfg` and a current value such as `_TZ3000_xr5m6kfg` when each is directly mapped to the same SKU. Never generalize either value to sibling products. Use the official family and concise capabilities in `name`, such as `Walter RGB+CCT Zigbee 9W LED Downlight (92 mm)`. Scan LUTs for isolated measurement dips; correct only scanner-confirmed outliers using the surrounding curve and regenerate both plots.

## Meross

Use the official Meross product code as the canonical profile ID, preserving its capitalization, for example `MSL120`. Meross integrations can report a lowercase internal device type with a hardware or protocol suffix, such as `msl120d 2.0.0`; preserve the exact confirmed value as a discovery alias rather than using it as the directory ID. When migrating an existing internal-value directory, also retain that former directory value in `legacy_ids`. Do not infer aliases for another regional, HomeKit or hardware variant from the shared `MSL120` product family. The official product page can group several bases and regions under one model, so retain the measured profile's provenance and do not force its peak or standby values to the rounded 9W and `<0.5 W` specifications. Use the official product description in `name` without repeating Meross or the canonical model.

## MOES

Prefer the complete single-device SKU in MOES' official webshop metadata, preserving suffixes such as `-MS` and `-EN`. A numeric Matter product ID such as `1412` and a shortened Zigbee value such as `ZB-LZD10-RCW` can be exact Home Assistant discovery identifiers; retain them as aliases and, after a directory migration, as `legacy_ids`. Do not use quantity SKUs ending in `-2` or `-4` as aliases for the single-device profile, and do not infer that a suffix can be removed from another product. Cross-check the original PR's physical description against the official variant and measured peak: the Matter profile linked as a GU10 lamp peaks near its official 5W rating, while the 10W downlight reaches 10W. Use those verified fitting, capability and wattage details in `name`; do not perpetuate an unsupported A19/E26 description copied during later cleanup. Home Assistant's parenthesized Matter presentation keeps the friendly model and numeric model ID separate, so never add the combined display string as an alias.

## Müller Licht

Prefer the exact article number printed on the measured lamp or published as `Artikel-Nr.` on Müller Licht's official product-information page. Treat generic Zigbee identifiers such as `ZBT-ColorTemperature`, `ZBT-ExtendedColor`, and `tint-ExtendedColor` as controller or capability families: they can be shared by different fittings, shapes, wattages, and article numbers, so do not use them as aliases without evidence that presenting multiple candidates is safe and sufficiently complete. Keep Home Assistant's separate `model` and parenthesized `model_id` as separate discovery values; never construct a combined alias. When Home Assistant reports a related catalog ID for the measured product, preserve only the exact ID as an alias and keep the official measured article number canonical. Müller Licht can reuse an article number and consumer EAN for a lower-power hardware revision; match dated specifications to the LUT peak and omit an ambiguous EAN. Do not store inner- or shipping-carton GTINs as multipack EANs. Use the official `tint` styling and a concise English product description in `name`, translating terms such as `Kerzenform`, `Reflektor`, and `Deckenleuchte` instead of mixing German and English.

The legacy `ZBT-ColorTemperature` GU10/350 lm measurement and retail code `43961` map to official article `404006`: the three-way converter mapping distinguishes it from E14 article `404008` and E27 article `404004` through the measured fitting and output, while Müller Licht's declaration groups `43961` and `404006` as the same 5.1W/350 lm GU10 variant. Keep `43961` as an exact alias and both former directory IDs in `legacy_ids`, but do not retain shared `ZBT-ColorTemperature` as a discovery alias.

## Nanoleaf

Use the exact official Nanoleaf model or SKU printed for the measured regional variant as canonical, preserving complete pack and fitting suffixes such as `-1A19E`. Nanoleaf documents both `NL...` model identifiers and `NF0...` SKU variations, so do not replace one verified code with a similar regional sibling. Home Assistant's Matter integration exposes the human-readable product label/name as `model` and the decimal Matter product ID separately as `model_id`; preserve exact values such as `Essentials A19`, `Essentials A19/A60`, and `67` as aliases, never as a constructed parenthesized string. These Matter values can be intentionally shared by A19/E26 and A60/E27 variants so PowerCalc can present both measured candidates; shared discovery metadata alone does not prove that their LUTs can be merged. Keep regional shape and fitting explicit in `name`, and scan low-brightness LUT plateaus for isolated transition spikes before accepting otherwise similar curves.

## Netatmo

Prefer Netatmo's exact printed or regulatory hardware model over a descriptive directory slug. The Netatmo integration can expose the current product name as Home Assistant's model; retain that exact value as a discovery alias while keeping the hardware model canonical. A marketing rename from Healthy Home Coach to Smart Indoor Air Quality Monitor does not by itself identify different hardware. When migrating a released descriptive directory to the verified model code, preserve the former directory in `legacy_ids`.

## NETGEAR

Use NETGEAR's exact printed model code as canonical and retain the official uppercase brand styling. An unmanaged network switch without a native Home Assistant integration can correctly use manual discovery and needs no speculative model or manufacturer aliases. Give the profile a concise product description that does not repeat the canonical model or manufacturer; distinguish port count, management type and uplink capability when those details separate nearby models.

## Neo CoolCam

Use the exact printed Z-Wave product model as canonical and do not infer regional or hardware-revision suffixes from sibling products. Preserve the exact manufacturer value reported by Home Assistant/Z-Wave as an alias, including its legal-entity wording. A smart-plug profile with `only_self_usage` intentionally records the plug's own relay-off and relay-on consumption rather than the attached load; retain that distinction and describe the product concisely as a Z-Wave smart plug.

## NodOn

Prefer the exact printed NodOn product code when the measured socket variant is known. When historical evidence establishes only the Z-Wave-certified family model, retain the exact discovery identifier such as `MSP-3-1-X1` rather than guessing a Type E or Schuko suffix. Preserve `ID-RF` as an exact Z-Wave manufacturer alias. A profile with `only_self_usage` represents the plug's own relay-off and relay-on consumption, not its metered downstream load.

## NOUS

Use NOUS's official A-series product code as canonical and retain the uppercase brand styling. A Tasmota device may report a branded template name such as `NOUS A1T`; preserve that exact value as a model alias, but never add generic firmware name `Tasmota` as a manufacturer alias. For Tuya-based Zigbee products, a generic model such as `TS011F` is safe only in combination with the exact manufacturer signature confirmed for the measured product, such as `_TZ3000_2putqrmw` for A1Z. Physical similarity to another Tuya plug such as BSD29 does not establish identity or justify an alias. A metering-socket profile with `only_self_usage` records only the socket electronics and relay consumption, not the attached load.

## Nuki

Use the exact Nuki manufacturer SKU as canonical when it is exposed by an official shop link or printed on the product or packaging. Preserve the exact Home Assistant model string, such as `Hardware Bridge`, as a discovery alias when the original Device Info confirms it. Keep the device serial number out of aliases. Use the official product category as the basis for a concise descriptive `name` without repeating the SKU or manufacturer; for the Bridge, a fixed profile represents the device's full draw and does not use `only_self_usage`.

## Oz Smart Things

Prefer the exact model code printed on the measured device or packaging. A product page can be updated in place for a later hardware generation, so do not replace or alias an older packaging model with a current V2 webshop SKU unless the label, Device Info or manufacturer documentation establishes that they are the same electrical product. Include verified functional details such as RGBW and nominal wattage in a concise display name without repeating the manufacturer or canonical model ID.

## Philips Air Care

Use the exact regional model reported by the device and printed on its label, such as `AC4236/14`. Replace the slash with an underscore only for the canonical directory name and preserve the exact slash-form value in `aliases`: `philips_airpurifier_coap` exposes the device status model in Home Assistant Device Info, so that alias is required for discovery. Do not add sibling region codes such as `/10` without evidence that Home Assistant reports them for the measured hardware, and keep these Philips air-care profiles separate from Signify / Philips Hue guidance. Use Philips' official English product-family wording for `name` without repeating the model code. The established `AC4236/14` profile uses Wi-Fi.

## Qubino

Prefer the exact Qubino ordering/model code printed for the measured regional device, such as `ZMNHYD1`, over the product-family name. Qubino distinguishes sibling suffixes by Z-Wave frequency, so do not add `ZMNHYD4`, `ZMNHYDA`, `ZMNHYDB`, or `ZMNHYDE` without evidence for the measured hardware. Z-Wave JS exposes the shared fingerprint `0x0002:0x0054` as `Smart Plug 16A`; preserve that exact label as a discovery alias. Keep the official product-family wording as the descriptive profile `name`.

## Reolink

Prefer Reolink's exact printed item/model code over a marketing-name directory: for example, the E1 Pro quick-start guide identifies item `E330`, while Home Assistant can report `E1 Pro (E330)`. Keep that full Device Info value as an alias. Preserve hyphenated camera codes such as `RLC-810WA` exactly. Reolink hardware-version values such as `IPC_566SD54MP` can span related product names or revisions and belong in aliases only when an integration actually reports them and the measured hardware is established. Camera power can change with infrared LEDs, spotlights, motors, streaming and detection activity; prefer separately measured subprofiles when a stable state entity exists, otherwise document that a fixed value is a long-term average.

## Rituals

Prefer Rituals' official article/trade code as canonical when it is explicitly mapped to the measured diffuser. Preserve exact Home Assistant discovery values such as manufacturer `Rituals Cosmetics` and model `The Perfume Genie 2.0` as aliases. Do not use the LOT number printed below the device QR code: Rituals treats it as device-specific support information rather than a model identifier. Use the official generation in the descriptive profile name without repeating the article code.

## ROBB

Use the complete `ROB_...` code exposed by the device as the canonical model, preserving separators and the final revision digit. Keep exact discovery manufacturer `ROBB smarrt` as an alias of library manufacturer `ROBB`. Sibling codes such as `ROB_200-017-0` and `ROB_200-017-1` remain separate unless product documentation and measurements establish equivalence. A self-usage smart-plug profile models relay-off and relay-on electronics consumption; use the documented load class only to make its descriptive name more specific.

## Roborock

Prefer a printed product model that official support data maps to the measured vacuum, such as `SDJQR01RR` for the first-generation Mi Robot Vacuum. Keep the exact internal model reported by Home Assistant as a discovery alias: current Roborock Device Info exposes the same internal value as both `model` and `model_id`, while older Xiaomi Miio devices use values such as `rockrobo.vacuum.v1`. Do not substitute a similar internal code: `roborock.vacuum.s5e` identifies the S5 Max, `roborock.vacuum.a10` identifies the S6 MaxV, and `roborock.vacuum.a15` identifies the S7. When official data establishes that SKU codes differ only by color and the power behavior is identical, choose one representative SKU as canonical and retain the other verified color SKU in `aliases`; for example, use white `S5E02-00` as canonical and black `S5E52-00` as an alias. Retain `rockrobo` as a manufacturer alias, but remove an alias identical to canonical `Roborock`. When the canonical identifier is an opaque printed code, include the recognizable product family in the descriptive name, for example `S5 Max Robot Vacuum and Mop` or `S6 MaxV Robot Vacuum and Mop with ReactiveAI`; this does not duplicate the canonical ID. Charging profiles should remain scoped to docked charging unless cleaning consumption was separately measured.

## Rye

Prefer the exact product model `SLZC3705-A` for the Rye 5W 450 lm E12 RGBCCT candle bulb. Preserve the released directory value `Candlebra` as both an alias and legacy ID, and preserve exact Tasmota template/module name `RYE Candlebra` as an alias. A Tasmota template name can be reflected in Home Assistant, but firmware brand `Tasmota` is not evidence that it belongs in the manufacturer's aliases; require exact Device Info before adding it. Do not describe the bulb as dusk-to-dawn unless the physical product is documented with an integrated ambient-light sensor; firmware scheduling does not establish that hardware feature. A measured peak around 4.1W is plausible for this nominal 5W product, and unusually low dimming values should be evaluated against the original repeated measurement discussion rather than discarded in isolation.

## Sengled

Prefer exact Sengled model codes documented in the manufacturer manual and reported over Zigbee, such as `E11-G13` and `E13-N11`. Do not add retail suffixes such as `W` or `WA` as aliases unless the measured label or exact Home Assistant Device Info establishes them. A manual that maps a retail code such as `E13-N11WA` to hardware model `E13-N11` can still establish that retail package's barcode for the canonical hardware profile; the retail suffix does not thereby become a discovery alias. Zigbee2MQTT may map several raw Zigbee models to one slash-separated display model; for the Element Extra Bright A19 family, preserve `E11-N13/E11-N13A/E11-N14/E11-N14A` as the grouped discovery value and also include all four individual raw values as aliases for integrations such as ZHA. Do not choose one member as canonical for an existing grouped measurement unless the measured product label identifies it. Keep profile names descriptive and omit the canonical model and protocol; verify color temperature against the exact manual because `E11-G13` is a 2700K soft-white bulb, not a 5000K daylight bulb.

## Shelly

Use the exact Shelly hardware model returned as `model` by `Shelly.GetDeviceInfo` for Gen2+ devices, or as `type` by the Gen1 `/shelly` endpoint, as the canonical profile ID. Keep human-readable product strings such as `Shelly Plus Plug S` as discovery aliases only when Home Assistant reports them, and never include the MAC-suffixed device ID. A documented revision may share calibration data when the manufacturer confirms that its changes are mechanical only. When such a revision is already an exact alias of the shared profile, its exactly mapped EAN may also be included to make that compatible package discoverable; do not copy the barcode to unrelated regional electrical variants. Treat a regional electrical variant as separate unless measurements of that variant or manufacturer evidence establish equivalent self-consumption; a shared product name or contributor expectation is insufficient, particularly when the nominal mains voltage differs. A linked profile is acceptable when an actual comparison measurement supports the equivalence, even if that comparison is less precise than the canonical measurement.

For no-neutral switches that require a bypass, distinguish the manufacturer's device-only consumption limit from a measured circuit value that includes bypass current. Store the declared device limit as `rated_power` when the repository convention represents a published upper bound numerically, but retain the higher measured fixed values and `only_self_usage: false` when the original setup explicitly included the required bypass. Do not reject those measurements merely because they exceed the device-only specification.

More generally, keep Shelly's published device-consumption limit separate from empirical profile values. Store a claim such as `< 1 W` numerically as `rated_power: 1.0`, while preserving a credible higher measurement; do not invent an accessory, tolerance, or measurement-error explanation when the available evidence only establishes that the values differ.

Shelly family pages may document one consumption limit across multiple exact API hardware codes. Preserve the reported code rather than replacing it with the first code on the product page: `SNSW-102P16EU` is a Plus 2PM variant alongside `SNSW-002P16EU`, and `SPEM-003CEBEU63` is the Pro 3EM package with 63A current transformers alongside the base `SPEM-003CEBEU`. A family rating may be used when a maintained device mapping confirms the variant and the profile measurements are compatible. Exclude separately powered or optional Add-On consumption when comparing the bare-device limit.

## Signify / Philips Hue

Prefer the exact Hue/Zigbee model code exposed by the Hue API, ZHA, or deCONZ, such as `LCA005`, `LCT016`, or `LTW004`, when it uniquely identifies the measured hardware. Zigbee2MQTT can normalize one or more of those raw codes to a Signify article, material, or retail model such as `9290022266A` or `8718696548738`; preserve that exact normalized value in `aliases` so the profile remains discoverable through that integration. A normalized alias may intentionally be shared by multiple raw-model profiles, in which case PowerCalc presents the candidates for manual selection.

Signify can reuse a base 12NC for a later, lower-power optical revision. A dated exact-12NC product sheet plus the profile creation date and measured maximum may identify the older revision without a surviving package label when the later revision's declared maximum is below the measured curve. For example, a 2022 `LTE002` LUT peaking at 4.75W matches the contemporaneous `9290022944A` specification of 5.2W/470 lm and cannot represent the later 4W/320 lm revision.

Do not retain a sibling's raw model code or normalized article code merely because an older mapping grouped it: once a distinct measured profile exists and the source mapping identifies it separately, remove the stale values from the sibling profile. For example, `LCA005` and `9290022266A` belong to the measured `LCA005` profile and must not remain aliases of `LCA001`. Preserve former released directory IDs in `legacy_ids` independently of discovery aliases. Multi-endpoint suffixes such as `_01` and `_02` are aliases only when the integration reports them for that exact luminaire.

Map light metadata through the exact Hue model, 12NC/article and region using Signify's official compatibility tables, product sheets, EPREL records, or product pages. Store verified single- and multipack barcodes when every pack contains that exact model. Do not add a newer revision's EAN merely because nominal specifications look equal, and resolve any existing article-alias collision before attaching metadata to either profile.

Prefer an exact EAN/12NC product page over an unqualified family-level catalog value when they conflict. Compare like electrical quantities: a measured AC mains input may legitimately exceed a power supply's rated DC output because of conversion losses, so that comparison alone does not invalidate the exact product's published light or load wattage.

Signify fixture sheets can show both a rounded marketed `total lamp output` and a higher technical output at 4000K. Use the technical 4000K value consistently for `lumens` when it is explicitly identified for the exact model; do not mix that convention with a per-module value or a sibling revision's marketing output.

Do not interpret malformed fixture-table fields as complete-device power when they expose internal LED counts and per-source wattage. Prefer the wattage embedded in the exact official product designation and corroborate it with a contemporaneous exact-12NC catalog row and the measured power range.

A Hue fixture article can remain unchanged while the bundled replaceable bulb is revised from, for example, 8.5W to 9W or 9.5W. The fixture EAN and verified lumen class can still be useful, but omit `rated_power` unless the measured bulb revision is established by dated packaging, the original PR, or a LUT peak that clearly distinguishes it.

For multi-light luminaires, distinguish a profile for the complete fixture from profiles for separately exposed Hue child entities. Apply total fixture lumens and input power only to a complete-fixture profile; keep per-lamp values on an individual child profile and do not copy a fixture GTIN's aggregate specifications onto every endpoint. Derive an endpoint's output from a fixture total only when official, generation-specific sources identify the exact fixture and every other included light source, so that the remainder is unambiguous; otherwise leave endpoint lumens unset.

For legacy Hue fixtures, distinguish the raw Zigbee model from the retail article and later Bluetooth revisions. For example, `LTP001` identifies the older 39W/3000 lm Cher pendant family (`4076130P7` black and `4076131P7` white), while `LTP003` and article `4033930P7` identify the Fair pendant. Likewise, older 28W Aurelle profiles `LTC013` and `LTC016` must not inherit the 19W/21W successor 12NCs, and different square-panel sizes must never be grouped. A current product URL can redirect to a lower-power successor under a new 12NC, so compare dimensions, EAN, LUT maximum and dated official catalog specifications before transferring current-page metadata. Finish variants with identical electrical specifications may share one profile and retain their exact articles and consumer EANs as aliases and barcodes.

Some Hue raw model IDs were reused across materially different retail generations. Establish the measured generation from the original contribution's exact article, integration data and product link, then attach wattage, lumen and barcode metadata only from that generation. An older established integration-normalized alias may remain solely for discovery compatibility when removing it would strand existing installations; explicitly document that it is not metadata provenance. For example, `4080248U9` was measured as Gradient Signe article `915005987201`, while normalized alias `9150058704` belongs to the older non-gradient Signe. Electrically equivalent finish and regional-plug variants of the measured generation can share the profile when Signify documents their identical specifications.

Treat legacy LivingColors model IDs as generation-specific even when later products reused familiar names. `LLC001` identifies the 15W Generation 2 family, while the later 10W/210 lm Iris products use other raw model IDs such as `LLC006` and `LLC010`. A plausible LUT maximum can corroborate the older rated power, but do not transfer the later Iris lumen figure or barcodes without an exact article mapping for the measured lamp.

For Philips/Mijia desk lamps, prefer the exact Philips product model as canonical and retain the Xiaomi/Mi Home device type as a discovery alias and legacy ID when migrating an existing profile. Distinguish luminous flux in lumens from illuminance in lux: the Mijia Philips Desk Lamp 3 manual maps device type `philips.light.sread3` to product model `9290029013`, but its `1900 lx` specification is center illuminance and must not be stored as `lumens`.

Do not infer a screw fitting from the A19 or A60 form factor. Verify the region, voltage and exact article: North-American `LWB014` hardware `9290011369B` is E26, 9.5W and 840 lm. When the same raw Hue model and power profile is documented for both E26 and E27 variants, store both fittings in the `socket` array and omit the fitting from the display name; `LWB004` is such a legacy Hue Lux case. Preserve an established integration-normalized alias even when it describes another regional article, because removing it would break discovery for integrations that still expose that normalized value.

Use the official English Hue product description in `name`, with consistent capitalization, `Bluetooth` spelled out, and compact dimensions and wattages such as `30x30cm` and `4.2W`. Do not include the canonical model code in the name. Review legacy Hue LUTs carefully. When only a few points are isolated near-zero readings or multi-watt jumps and their neighbors establish a smooth curve, correct them locally with the LUT validator's expected-value interpolation instead of reopening a very old profile PR. Do not automatically correct a LUT with many or structurally clustered anomalies; leave it for targeted remeasurement or a raw-data explanation.

## SmartHomeShop

Use the model segment from the official ESPHome `esphome.project.name` as the canonical profile ID. For example, `smarthomeshop.ultimatesensor_mini` identifies UltimateSensor Mini V1, while V2 deliberately uses `smarthomeshop.ultimatesensor_mini_v2`. Keep that exact underscore-form identifier for discovery and use the official product name plus hardware generation as the display name. Treat hardware generations and power-relevant options such as an active particulate-matter sensor as distinct unless measurements establish equivalent consumption.

## SNOOZ

Prefer the exact model printed on the underside of the measured device, such as `SNOOZ-US-2`. Keep the official `SNOOZ` brand capitalization in manufacturer metadata and use the concise product-family name without repeating the brand or canonical model. Home Assistant's SNOOZ integration currently creates device registry entries without manufacturer or model fields, so adding guessed aliases does not make these profiles automatically discoverable.

## SONOFF

Use the exact SONOFF model from the label or official manual, including punctuation such as `B02-B-A60`; do not collapse it to `B02BA60` or replace a concrete code such as `B1` with a descriptive directory slug. Preserve former released directories in `legacy_ids` and retain a former discovery-compatible directory value as an alias when it is unique to that profile. For Zigbee devices, retain exact raw model identifiers as aliases when the current device definition maps them to the measured product: `01MINIZB` maps to `ZBMINI`, while `MINI-ZBD` is a confirmed `ZBMINIR2` variant. Use the official product-family wording for the display name without repeating the SONOFF brand or canonical model ID. Older SONOFF manuals can also provide model-specific light metadata; for example, the B1 manual explicitly lists 6W and 600 lm. Add a retail barcode only when the listing or packaging identifies the exact model and pack size.

## Sony

Use the exact Sony model code from the product label or official support documentation, such as `LF-S50G`. For Cast-connected products, confirm the exact `model_name` exposed in Home Assistant before adding a discovery alias; do not infer aliases from the shortened family name used in a support-page title. Use Sony's concise official product description for `name` while omitting both the manufacturer and canonical model code.

## Sylvania

Prefer the exact model printed on the Sylvania lamp, such as `A19CCECWF` or `40A19FILCCLWIFI`, over a retail SKU or descriptive catalog abbreviation. Preserve an exact Tuya cloud `product_id` as an alias when it was retrieved for the measured product, but recognize that LocalTuya and Tuya Local may expose only a device-unique ID and therefore cannot currently use that alias for discovery. Record the firmware context: a LUT measured with stock Tuya firmware is not automatically valid after flashing ESPHome, Tasmota, or OpenBeken because channel behavior and brightness curves can change. Use a concise `SMART+ Wi-Fi` product description with compact wattage and temperature notation.

## Tasmota

Treat `Tasmota` as an integration-reported firmware manufacturer namespace, not as the physical manufacturer. Home Assistant's Tasmota integration uses the firmware-reported module or active-template name as the device model. A symlink under `profile_library/tasmota` may therefore route that exact model to the canonical hardware profile, but only when the template/module is uniquely mapped to that hardware. Keep the hardware's own manufacturer and model canonical.

For new routes, do not add a Tasmota mapping for Zigbee-only hardware: Tasmota does not run on such an endpoint, and Zigbee devices paired through Tasmota are not discovered by the HA Tasmota integration. However, do not remove an already released Tasmota route solely because current official discovery documentation cannot produce it. Existing installations may retain stored selections or depend on manually supplied MQTT Device Info, and `legacy_ids` cannot bridge a change of manufacturer namespace. Preserve such a route as a compatibility exception unless a verified migration path exists; it is not precedent for adding similar new routes.

## TP-Link

Use the exact model exposed by Home Assistant as the canonical directory. TP-Link's integration may report a family code such as `L510`, `L530`, `L900`, or `L920` even though the retail products add a fitting or strip-length suffix such as `L510E`, `L510B`, `L900-5`, or `L920-5`. Do not add those longer retail variants as aliases unless HA actually exposes them; a shared family profile is appropriate only when the variants were confirmed equivalent for power consumption.

The fitting suffix can distinguish otherwise electrically identical bulbs. One family profile may list both fittings when exact Device Info reports the shared family model, the contributor explicitly measured one fitting on behalf of both, and an official product-information sheet groups the variants at the same hardware revision with identical electrical specifications. Keep revision-specific ratings: for example, Tapo L510E/L510B hardware 3.0 is specified at 8.3W, while older or different revisions must not be folded in solely because they share `L510`.

Keep exact region-qualified integration values such as `HS100(EU)`, `HS300(US)`, or `KP115(AU)` as aliases after confirming the same power behavior. Matter may expose a numeric product ID instead of the Kasa/Tapo model; retain a confirmed ID such as `261` or `262` as an alias and `Tapo` as a manufacturer alias. Use the official product title for `name` after removing redundant manufacturer and model text. TP-Link's own titles vary between `WiFi` and `Wi-Fi`, so verify the individual product page instead of applying a global spelling rewrite.

For light strips, confirm the measured length behind a family model such as `L900` or `L920` before copying specifications from retail variants such as `L900-5` or `L920-5`. An exact originating commit or Device Info mapping may establish the length; without it, do not assume the base family always means 5 m.

## Third Reality

Use the exact model exposed by Home Assistant together with the official product code and packaging barcode. Third Reality has reused `3RCB01057Z` for the older A19 `ZL1` bulb and a later BR30 `ZB3` bulb with different light output and UPC. Do not treat the shared model string as proof of electrical equivalence; use the originating profile evidence and exact barcode to keep their metadata separate.

## Treatlife

Use the printed Treatlife product code as the canonical directory ID. When the Tuya integration reports manufacturer `Tuya` and an opaque but exact product model for the measured device, preserve both values as manufacturer and model aliases so discovery still works. Do not infer an opaque identifier from another Tuya product or add a generic descriptive Tuya model.

## Tuya and white-label devices

Tuya is an IoT platform, not normally the consumer brand that should own a profile. Move a profile to the verified brand when the packaging, official documentation, or original contribution establishes that brand; leave genuinely unbranded or unresolved hardware under Tuya. For a resolver-only manufacturer cleanup, preserve `Tuya` as a manufacturer alias on the target and preserve the exact former model directory as a normal model alias when the canonical model also changes. Audit every manufacturer sharing `Tuya` and require the complete old manufacturer/model pair to resolve to exactly one profile. Do not use this exception for generic identifiers shared by multiple products; if the pair collides, keep the profile manual or wait for a product-specific identifier. Apply the same brand-versus-platform distinction to eWeLink.

Shared Tuya and Zigbee models such as `TS011F`, `TS0121_plug`, and `TS0505B` can represent many white-label products with different ratings, plug types, or light engines. Do not add one reseller's brand, barcode, or product specifications to such a profile without exact evidence tying the measured unit to that package. A genuinely generic or unbranded profile may remain under Tuya with a functional name.

Do not infer connectivity from the Tuya platform name alone. Preserve profile-specific evidence: the established `TS011F`, `TS0121_plug`, `TY-A60-15W`, and `TYC-300mm-Z` profiles are Zigbee, while `T17` is Wi-Fi; verify every other Tuya identifier independently because similar-looking IDs can use different transports.

## Tripp Lite

NUT devices can expose only a generic `device.model` such as `Tripp Lite UPS`, even when the physical UPS model is unknown. Preserve that exact discovery model for a generic calculation profile whose nominal capacity and load come from device entities; do not invent a hardware-specific model. Give the profile a functional name such as `UPS Power Estimate` instead of repeating the discovery identifier.

Record the actual UPS-side connection rather than the NUT host's network connection. The established generic Tripp Lite UPS profile uses Ethernet; APC `BE650G1` uses USB. Do not turn a UPS output capacity expressed in VA or W into `rated_power`, because it is not the device's own consumption rating.

## Trust / KlikAanKlikUit

Use the exact official `ZLED-...` product code as the canonical profile ID and the official functional product description without the manufacturer or model in `name`. Trust smart-home products can also be sold under KlikAanKlikUit branding; keep manufacturer aliases tied to exact Home Assistant Device Info rather than inferring them from co-branding alone.

## Velux

Use `KLF 200`, including its space, for the gateway profile: Home Assistant registers manufacturer `Velux` and model `KLF 200` explicitly. Use the official functional description `Home Automation Interface` as the profile name instead of repeating the gateway model.

## VeSync / Levoit

Keep exact VeSync discovery models such as `Core200S`, `Core300S`, and `Core400S` as the canonical profile IDs. Official Levoit pages may format the same family with a space or a retail `-P` suffix; do not add those variants as aliases unless Home Assistant reports them. Use the concise product name `Smart Air Purifier` without repeating the model or either brand.

The VeSync integration can expose preset modes without a usable percentage. Preserve a measured, device-specific fixed mapping for such a mode instead of deriving it from the manual fan-speed states; for example, a deliberately conservative auto-mode value may be appropriate when the device controls its own speed and HA reports no percentage. Levoit can revise the rated power under the same `Core...S` family. When the measured maximum closely matches an official manual or regulatory rating, keep that measured revision's rating; a lower current store-page value establishes a later revision and does not by itself require contributor follow-up.

## Wyze

Use the exact alphanumeric model reported by the Wyze integration, such as `WLPA19C` or `WLPP1CFH`, as the canonical profile ID when the original measurement provides exact Home Assistant Device Info. Keep `WyzeLabs` as a manufacturer alias when that is the integration-reported value. A cloud-reported model can span retail packages or hardware generations: require the device/package label before attaching a pack UPC or generation-specific connectivity, especially for `WLPP1CFH` versus single-pack `WLPP1CFH-1`. Derive a concise name from the official product title by removing the manufacturer and avoiding the model, for example `Bulb Color` or `Plug`.

## Xiaomi ecosystem

Prefer a Xiaomi hardware model that official product specifications or compliance documentation maps uniquely to the measured product, such as `MJDPL01YL` for the EU Mi Smart LED Bulb Essential or `BPLDS02DM` for Mi Smart Standing Fan 2. Keep the official marketing title as `name` once the canonical directory is the hardware code, and preserve a released marketing-name directory in `legacy_ids` after migration.

Treat `yeelink.*`, `lumi.*`, `dmaker.*`, and co-branded identifiers such as `philips.light.*` as integration or protocol identifiers until their scope is proven. When official documentation maps one uniquely to a printed hardware model, use that model canonically and retain the exact integration identifier as an alias; preserve it in `legacy_ids` as well when it was a released directory. Do not add a value such as `yeelink.light.color3` as an alias when the original review establishes that it is shared by multiple physical bulbs. When an integration exposes a combined gateway description containing several hardware codes and the measured variant cannot be isolated, retain the established protocol ID as canonical, preserve the exact combined discovery string as an alias, and give the profile a functional name rather than repeating the protocol ID.

## Yeelight

Use the printed Yeelight/Xiaomi hardware code, normally an `YL...` or `MJ...` value, as the canonical profile ID. Preserve an exact Yeelight integration identifier as an alias only when it uniquely identifies that hardware; known shared identifiers such as `color1`, `color4`, and `bslamp1` are unsafe because Home Assistant maps each to multiple physical models. Exact combined Mesh model strings may be retained alongside their unique `yeelink.*` component. Add `Xiaomi` as a manufacturer alias when exact Home Assistant Device Info for Yeelight Mesh hardware reports Xiaomi. Keep names concise, strip redundant `Xiaomi` or `Yeelight` branding, and use compact wattage such as `5W`.

Verify light metadata for the exact hardware code, region and strip length. The same Yeelight model can have different regional lumen ratings, and a base strip plus extensions can have materially different total power. A barcode is safe only for the exact packaged configuration represented by the profile; do not use it to imply that every supported extension length shares one rating.

Resolve reused or closely related Yeelight identifiers against exact-model, region-specific manufacturer documentation before changing measured metadata. For example, Yeelight's 220V manual identifies `YLDP02YL` as a 10W, 600 lm E27 lamp, while differently rated 9W sources concern another regional or voltage revision. Likewise, the official Yeelight and Xiaomi pages identify `YLXD01YL` as the 28W, 1800 lm Wi-Fi/Bluetooth ceiling light; do not transfer a later 23W successor's rating to that profile.

## Ynoa

Prefer the printed `LA-...` article number as the canonical profile ID. When Zigbee2MQTT maps a unique raw `ZBT-...` Zigbee model to that article, retain the raw value as an alias so discovery also works when an integration exposes the Basic-cluster model directly. Use a concise functional product name and compact wattage; for example, article `LA-GU10-RGBW` is a `Smart LED GU10 RGBW Bulb (4.8W)` and uniquely maps from `ZBT-RGBWLight-M0000`.

## YouLess

Use the exact model returned by the device, such as `LS120`, as the canonical profile ID. Home Assistant exposes this value directly as Device Info `model` with manufacturer `YouLess`, so no redundant alias is needed. Keep `LS110` separate because it is a different hardware generation, and do not treat PVOutput or Enologic firmware as separate model identifiers. A `config_entry` discovery profile is appropriate for the monitor itself when the integration exposes multiple sensor entities for one physical device.

## Zemismart

Prefer a printed model or article number when official documentation uniquely identifies the measured product. If an older released profile uses a descriptive slug and neither the product page nor the firmware reference provides a reliable product model, retain that identifier until label evidence is available; do not replace it with an ecommerce-internal numeric SKU. ESPHome node and entity names are user-configurable and therefore are not discovery aliases. When the measurement used replacement firmware and the source documents different power behavior from stock firmware, identify that scope in the display name, for example `Moon Lamp (ESPHome)`.

Replacement ESPHome firmware does not by itself change the physical transport category. Record the established Moon Lamp profile as Wi-Fi while keeping the firmware scope in its display name.

## Zengge

Magic Home and `flux_led` values such as `Bulb RGBCW (0x35)` describe a protocol/controller class and can be shared by different physical products. Prefer the exact hardware model shown for the measured device in Home Assistant Device Info, retain the complete protocol descriptor as a discovery alias, and preserve a replaced released directory in `legacy_ids`. Do not infer a hardware model from another device with the same protocol type or use an Amazon ASIN as the manufacturer model. Use a concise product description for `name` without repeating either identifier.

## Zipato

Retain an established Z-Wave model such as `RGBWE2` when it is the exact discovery value and the measured regional suffix cannot be proven. Zipato's Bulb 2 documentation inconsistently uses `rgbw2.eu`, `rgbwe2.eu`, and wildcard SKU `rgbw2.*`; do not select or add one of these regional values by inference. `RGBW Bulb V2` is a suitable concise product name because it identifies the official Bulb 2 generation without merely repeating the canonical code. Preserve a counterintuitive LUT curve when the original contributor manually reproduced the readings; a smoothness warning alone does not justify replacing verified device behavior.

## WiZ

Apply these rules when reviewing or normalizing WiZ and WiZ-powered Philips Smart LED profiles:

1. Prefer the exact WiZ/Signify material or article number (12NC) as the canonical directory ID, commonly a `929...` value. Confirm it on an official WiZ product page, product or packaging label, or equivalent manufacturer documentation. Do not substitute the retail EAN/GTIN for the 12NC or describe an EAN as the model number. Retain another concrete printed manufacturer model, such as a regional alphanumeric code, when no more authoritative 12NC can be established.
2. Home Assistant's WiZ integration derives its displayed `model` from the device `moduleName`. Values such as `SHRGB`, `SHRGBC`, `SHRGB1C`, `SHTW`, and `SHDW1` describe a controller/module configuration and can be shared by products with different fittings, wattages, shapes, or electronics. Do not use such a value as the canonical directory ID when a specific article number is known.
3. Add the exact `SH...` value reported for each measured device to `aliases`, even when another WiZ profile uses the same alias. This is an intentional WiZ exception: without that alias the article-number profile cannot be discovered, and PowerCalc can present all matching profiles for the user to choose from. Require evidence from Home Assistant Device information, diagnostics, the original profile PR, or the raw `moduleName`; do not infer an alias merely from advertised color capabilities.
4. Keep a former `SH...` directory value in `aliases` because it remains a reported discovery identifier. Do not keep a non-reported internal slug as an alias.
5. Never merge profiles merely because they share an `SH...` alias. Establish that they are the same physical product using the exact 12NC, label, official specifications, and where useful an exact EAN mapping. Shared module codes alone are evidence of discovery ambiguity, not hardware equivalence.
6. For confirmed duplicate physical products, consolidate under the verified article number and retain the most credible calibration data. Compare supported modes, LUT completeness, measurement setup, standby power, and peak draw against the rated power. A complete color-temperature and HS measurement reaching a plausible rated draw is normally preferable to an incomplete LUT with an implausibly low maximum; preserve relevant authorship and measurement provenance when consolidating.
7. If the exact article number or duplicate relationship remains uncertain, ask for the product or packaging label, exact Home Assistant Device information, originating integration, and full `moduleName`. Keep the established profile available until the evidence supports a migration; do not invent a regional 12NC suffix from a similar product page.
8. Spell the manufacturer `WiZ`. Use the official product description in `name`, distinguish WiZ-branded and Philips Smart LED products where that branding identifies the product line, and keep EANs and module codes out of the display name unless they materially clarify unresolved source evidence.
9. Treat a trailing article revision such as `A` or `AA` as significant when mapping barcodes. Keep base, `A`, and `AA` EAN sets separate unless an official declaration explicitly maps the same pack to each revision; an otherwise matching lumen and wattage rating is not sufficient.

## Sonos

Apply these rules when reviewing or normalizing Sonos profiles:

1. Distinguish three identifiers: the existing Powercalc directory ID, the manufacturer article/model number printed on the device or packaging, and the Sonos `modelNumber` commonly formatted as `S...`. Home Assistant's Sonos integration exposes the speaker `modelNumber` as `model_id` and the product name, with the `Sonos ` prefix removed, as `model`. Do not treat the S-code as the printed article number without separate manufacturer evidence.
2. For an existing profile, preserve its directory ID unless the review explicitly establishes and requests a canonical migration. A newly verified S-code belongs in `aliases`; it does not by itself justify renaming the directory.
3. Add an exact S-code to `aliases` only when device information, diagnostics, the original profile PR, or equivalent device evidence maps it one-to-one to the measured hardware generation. Do not infer the code from the marketing name or reuse a code across generations merely because Sonos presents them as one product family.
4. Treat generation-sensitive products as ambiguous until confirmed. This commonly affects One, One SL, Beam, Play:5, Sub, and IKEA SYMFONISK products. Ask for the exact Home Assistant `model` and `model_id`, the originating integration, the printed article/model number, and the hardware generation before adding an alias or sharing calibration data.
5. Keep distinct Sonos generations or product lines separate even when their names are similar. In particular, `Play:5` and `Five` are different products: never use `Five` as an alias for a Play:5 profile when a separate Five profile exists. Check every proposed name alias case-insensitively against both directory IDs and aliases to avoid discovery collisions.
6. An explicit generation in the original contributed profile or a contributor's confirmation can establish the measured generation for the display name. It does not by itself establish the Sonos S-code or a regional/color package barcode; require exact Device Info or label evidence for those identifiers.
6. A generic value such as `Sub` is insufficient evidence for a generation-specific profile. A bonded Sub may not expose standalone Home Assistant Device information, so request the physical label or packaging code and the generation rather than guessing from the household or parent speaker.
7. Use official product capitalization in `name`, omit the manufacturer, and format generation labels consistently, for example `Sub (Gen 1)`, `SYMFONISK Floor Lamp`, and `SYMFONISK Table Lamp`. Add a product name to `aliases` only when an integration actually reports it as a discovery identifier and it cannot collide with another profile.

When the printed article number, S-code, product name, or generation cannot be reconciled, keep the established profile unchanged and request contributor evidence. Do not add every historically associated S-code to a generic profile as a fallback.

## Useful primary references

- [Powercalc library structure](../../../../docs/source/library/structure.md) explains that the directory is the model ID and aliases are alternate discovery identifiers.
- [Powercalc measurement output guidance](../../../../docs/source/contributing/measure/output.md) requires the exact model identifier rather than the marketing name.
- [Powercalc Matter limitations](../../../../docs/source/library/matter-limitations.md) explains why Matter product IDs can be unsafe for automatic discovery.
- [IKEA's E2499 product page](https://www.ikea.com/de/en/p/varmblixt-led-table-wall-lamp-dimmable-smart-white-glass-colour-and-white-spectrum-70612940/) is an example of the **Model identifier** field in IKEA technical product information.
- [IKEA's TRETAKT manual](https://www.ikea.com/nl/nl/manuals/tretakt-stekker-smart__AA-2396101-2-2.pdf) identifies the 16 A European plug as type `E2204`; a separate [regional manual](https://www.ikea.com/ch/en/manuals/tretakt-plug-smart__AA-2437430-1-100.pdf) identifies the 10 A variant as `E2214`.
- [IKEA's LED2101G4 declaration](https://www.ikea.com/no/en/dec_of_conformity/tradfri-led-bulb-e14-470-lumen-smart-wireless-dimmable-white-spectrum-globe__AA-2378889-1-1.pdf) confirms the official type designation independently of the product description.
- [Zigbee2MQTT's IKEA device definition](https://github.com/Koenkk/zigbee-herdsman-converters/blob/master/src/devices/ikea.ts) records exact Zigbee descriptions separately from its normalized model values, including the combined TRADFRI driver identifier.
- [Shelly device information API](https://shelly-api-docs.shelly.cloud/gen2/ComponentsAndServices/Shelly/) documents `Shelly.GetDeviceInfo.model`.
- [Shelly Gen1 API documentation](https://shelly-api-docs.shelly.cloud/gen1/) documents the `/shelly` device-information response and its `type` model identifier.
- [Shelly Plus Plug S V2 documentation](https://www.shelly.com/blogs/documentation/shelly-plus-plug-s-v2) identifies `SNPL-10112EU` and describes its changes from the earlier model as mechanical.
- [Shelly Plus 2PM UL documentation](https://us.shelly.com/blogs/documentation/shelly-plus-2pm-ul) identifies the distinct 120V `SNSW-002P15UL` model.
- [The original Shelly Plus 2PM profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/3861) records measurements from EU model `SNSW-102P16EU` and adds the UL model based on an unverified expectation of equal self-consumption.
- [The Shelly Plus Plug US comparison PR](https://github.com/bramstroker/homeassistant-powercalc/pull/3356) records an approximate measurement comparison before the US model was linked to the EU profile.
- [Signify's effects-compatible lights table](https://www.assets.signify.com/is/content/Signify/Assets/hue/global/20221019-effects-compatible-lights.pdf) maps Hue Zigbee model identifiers to Signify article and product codes.
- [The Zigbee2MQTT Philips device definitions](https://github.com/Koenkk/zigbee-herdsman-converters/blob/master/src/devices/philips.ts) show the current normalization from raw Hue `zigbeeModel` values to the model exposed by Zigbee2MQTT.
- [The original LCA005 profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/3432) confirms exact Zigbee model `LCA005`, article code `9290022266A`, and the decision to remove those values from `LCA001` once a distinct profile existed.
- [The original Signify alias synchronization PR](https://github.com/bramstroker/homeassistant-powercalc/pull/3798) documents the use of Signify's official mapping table for discovery aliases.
- [Philips' Hue catalog for Mexico](https://images.philips.com/is/content/PhilipsConsumer/PDFDownloads/Mexico/ODLI20150604_001-UPD-es_MX-Folleto_Connected_Lighting_Jun2015.pdf) documents legacy Hue Lux article `433714` as a 9W, 750 lm A19 lamp with an E26/E27 base, while [the original LWB004 profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/25) records that the measured unit was E27.
- [The EPA ENERGY STAR product record for `9290011369B`](https://www.energystar.gov/productfinder/product/certified-light-bulbs/details/2300261) documents E26, 9.5W and 840 lm and associates retail article `453100`; [the original LWB014 profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/735) identifies the measured unit as an A19 lamp, and current Zigbee device data maps raw model `LWB014` to that hardware family.
- [The official UltimateSensor Mini firmware](https://github.com/smarthomeshop/ultimatesensor-mini) defines distinct ESPHome project identifiers for V1 and V2 and documents their different hardware.
- [The original SNOOZ-US-2 profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/3291) records the model from the measured device's underside.
- [Home Assistant's SNOOZ fan implementation](https://github.com/home-assistant/core/blob/dev/homeassistant/components/snooz/fan.py) currently creates device information without manufacturer or model fields.
- [The official SNOOZ product page](https://getsnooz.com/products/snooz-white-noise-machine) identifies the product family as the SNOOZ Original white noise machine.
- [SONOFF's official Wi-Fi smart LED bulb page](https://itead.cc/product/sonoff-wi-fi-smart-led-bulb/) identifies the punctuated `B02-B-A60` model and distinguishes it from RGB model `B05-B-A60`.
- [The SONOFF B1 manual](https://sonoff.ee/wp-content/uploads/2019/04/Instrukcija-B1-EN-LT-LV-EE.pdf) identifies the older RGB bulb's model as `Sonoff B1`.
- [The current Zigbee2MQTT SONOFF definitions](https://github.com/Koenkk/zigbee-herdsman-converters/blob/master/src/devices/sonoff.ts) map raw identifiers `01MINIZB` and `MINI-ZBD` to `ZBMINI` and `ZBMINIR2` respectively.
- [Sony's official LF-S50G support page](https://www.sony.com/electronics/support/speakers-wireless-speakers/lf-s50g) identifies `LF-S50G` and describes it as a Google Assistant built-in wireless speaker.
- [Sylvania's SMART+ Wi-Fi device catalog](https://assets3.ledvanceus.com/media/resource/original/asset-13109863) identifies the full-color A19 product family, product number `75663`, 2700–6500K range, and official `SMART+ WiFi` branding.
- [The original A19CCECWF PR](https://github.com/bramstroker/homeassistant-powercalc/pull/2018) and [40A19FILCCLWIFI PR](https://github.com/bramstroker/homeassistant-powercalc/pull/2019) record the printed lamp models, exact Tuya product IDs, LocalTuya limitation, stock-firmware measurements, and reflashing caveat.
- [Tasmota's Home Assistant documentation](https://tasmota.github.io/docs/Home-Assistant/) states that the active template name becomes the module name reflected in Home Assistant and that Zigbee devices paired in Tasmota are not discovered by the HA Tasmota integration.
- [NOUS's official A1T page](https://nous.technology/product/a1t.html) identifies A1T as a Wi-Fi socket with Tasmota, while its [A1Z page](https://nous.technology/product/a1z-1.html) identifies A1Z as a Zigbee socket.
- [Teckin's official SB50 product page](https://de.teckinhome.com/products/teckin-sb50-alexa-intelligente-gluhbirne) identifies `SB50` as a dimmable Wi-Fi RGB bulb, while [the original profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/1237) records that the measured lamp was flashed to ESPHome.
- [The original Telefunken profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/3494) records the measured `XF32E411M`, its firmware-dependent picture profiles, and the limited Home Assistant Device Info, while the linked [technical datasheet](https://www.prad.de/technische-daten/fernseher-datenblatt/telefunken-xf32e411m/) confirms the full model code.
- [Third Reality's Smart Color Bulb ZL1 manual](https://3reality.com/wp-content/uploads/2024/10/Smart-Color-Bulb-ZL1_UM_20240924.pdf) confirms the official product name, while [the original profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/3314) records HA model `3RCB01057Z` and manufacturer `Third Reality, Inc`.
- [python-kasa's discovery documentation](https://python-kasa.readthedocs.io/en/stable/guides/discover.html) demonstrates the integration-facing `L530` family model, while the original [L510 PR](https://github.com/bramstroker/homeassistant-powercalc/pull/3824) and [L920 PR](https://github.com/bramstroker/homeassistant-powercalc/pull/2565) record HA family models for retail variants with fitting or length suffixes.
- [The original P110M PR](https://github.com/bramstroker/homeassistant-powercalc/pull/3897) records TP-Link model `P110M` alongside Matter manufacturer `Tapo` and product ID `261`; [PR #4100](https://github.com/bramstroker/homeassistant-powercalc/pull/4100) adds the verified second Matter product ID.
- [TP-Link model-number guidance](https://www.tp-link.com/ae/support/faq/2053/) shows where the product model is printed and exposed in its apps.
- [Treatlife's support page](https://www.treatlife.tech/apps/help-center) identifies `SL10` as the Multicolor Bulb model; [the original SL10 profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/2781) records exact Tuya Device Info model `odsywzgrtvaz3b79` and manufacturer `Tuya` for the measured bulb.
- [The original Antela profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/1915) records exact HA model `ANTELA-9W-RGB+CCT`, opaque Tuya product ID `bf0f80ef7db65f56bfwkpm`, and retail item B09VKP5GLQ. Exact-ASIN records consistently establish E14, 806 lm and a marketed 9W variant, but disagree between package model numbers such as `AN-EU-A60-E14-2pack-TD` and `A60(E14)-PA-WIFILIC-TY-RGBCW`; retain the exact HA model canonically until the measured label is available.
- [The original Forever Light profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/3104) identifies `FLSP10A`; [TelforceOne's product listing](https://sklep.telforceone.pl/en-gb/product/2280194/2295946/forever-light-smart-plug-wi-fi-240v-10a-flsp10a) confirms the Forever Light product code.
- [The Hifree T21 manual](https://fccid.io/2APQM-T21/Users-Manual/User-manual-5877559.pdf) identifies model `T21`; [the original profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/3850) maps Tuya product ID `8jz6rasdn7xct1oq` to exact retail item B09BQ637HP. That package is reported as 8W with UPC `734598418113`, whereas another T21 revision is documented as 7W/400 lm, so revision metadata must not be mixed.
- [KOBI's official catalog](https://kobi.pl/wp-content/uploads/1683/97/KATALOG-2023_KOBI-LIGHT_MNIEJSZY.pdf) maps index `KAGS14RGBCTW` to the 14W, 1400 lm RGB+CCT bulb and EAN `5902201305425`; [the original KOBI and Sylstar profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/1852) records both measured products and maps the Sylstar retail model `SL-CL-24` to HA model `SYS-C24W-CW`.
- [Electro Cirkel's declaration for Action article 3004154](https://docs.calex.eu/doc/3004154.pdf) confirms factory model `MK-020011009068`, EAN `8712879151074`, 2.2W and 210 lm; [the original LSC moodlight PR](https://github.com/bramstroker/homeassistant-powercalc/pull/1735) maps that article to discovery value `CS3S`.
- [Sunco's official G25 product page](https://www.sunco.com/products/g25-smart-bulb-wifi) identifies exact SKU `G25_S-5W-27K_5K-1PK`, E26, 5W and 450 lm; [the original profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/1988) links the measured bulb to that product and records Tuya model `SUNCO-5W_I2C_RGBCW`.
- [Blakadder's eWeLink EZ-E14 device record](https://zigbee.blakadder.com/eWeLink_EZ-E14.html) maps model `EZ-E14` to exact Zigbee identifier `CK-BL702-AL-01(7009_Z102LG03-1)`, E14 and 5W; [the original profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/3008) records the measured short model `CK-BL702-AL-01`.
- [Treatlife's exact SL20 product record](https://device.report/treatlife/v8-sl20) identifies E26, 9W, 800 lm and four-pack UPC `728458730453`; [the original SL20 profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/842) records the measured profile.
- [The original Tripp Lite UPS profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/4067) records exact HA NUT Device Info manufacturer `Tripp Lite` and model `Tripp Lite UPS`, including the firmware's trailing whitespace and the absence of a specific physical model.
- [Trust's ZLED-G2705 datasheet](https://dezlwerqy1h00.cloudfront.net/Media/Datasheets/71159-Trust%20Smart%20Home-ZLED-G2705-Datasheet_en.pdf) identifies the exact product code and official `Zigbee Dimmable LED Spot` description; [the original profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/1547) records a two-spot measurement whose stored LUT is normalized to the single-spot draw.
- [Home Assistant's Velux integration](https://github.com/home-assistant/core/blob/dev/homeassistant/components/velux/__init__.py) registers the gateway as manufacturer `Velux` and model `KLF 200`; [VELUX's official KLF 200 page](https://www.velux.com/klf200) identifies it as an interface for advanced control.
- [The original Core 200S and Core 300S profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/4234) records exact HA VeSync models `Core200S` and `Core300S`; [the Core 400S PR](https://github.com/bramstroker/homeassistant-powercalc/pull/4489) documents verified auto-discovery and why its auto mode intentionally uses a fixed fallback. Levoit's official [Core 200S-P](https://levoit.com/products/core-200s-smart-true-hepa-air-purifier), [Core 300S-P](https://levoit.com/products/core-300s-smart-true-hepa-air-purifier), and [Core 400S-P](https://levoit.com/products/core-400s-smart-true-hepa-air-purifier) pages show the retail product naming variants.
- [The original Wyze Bulb Color PR](https://github.com/bramstroker/homeassistant-powercalc/pull/3898) records exact HA model `WLPA19C` and manufacturer `WyzeLabs`; [the Wyze Plug PR](https://github.com/bramstroker/homeassistant-powercalc/pull/3899) records `WLPP1CFH` with the same manufacturer value. Wyze's [WLPP1CFH manual](https://support.wyze.com/hc/en-us/article_attachments/22823886896155) documents Wi-Fi and Bluetooth but does not prove the contributor's package generation, so use its label rather than inferring a UPC from the integration model.
- [Xiaomi's Mi Smart Standing Fan 2 specifications](https://www.mi.com/global/product/mi-smart-standing-fan-2/specs/) identify model `BPLDS02DM`; [Xiaomi's compliance table](https://www.mi.com/global/support/terms/declaration/) maps Mi Smart LED Bulb Essential to `MJDPL01YL`. [The bulb profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/3478) records the generic `yeelink.light.color3` Device Info value and explains why it is unsafe for discovery, while [the gateway PR](https://github.com/bramstroker/homeassistant-powercalc/pull/3950) records the exact combined XiaomiGateway3 model string.
- [Xiaomi's Philips Wi-Fi bulb manual](https://i01.appmifile.com/webfile/globalimg/Global_UG/Mi_Ecosystem/Philips_Wi-Fi_bulb_E27_White/fr_V1.pdf) identifies hardware model `9290020093`; [PR #2776](https://github.com/bramstroker/homeassistant-powercalc/pull/2776) records exact Home Assistant model `philips.light.hbulb` for that measured product.
- [Home Assistant's Yeelight documentation](https://www.home-assistant.io/integrations/yeelight/) maps integration identifiers to one or more hardware models and exposes which values are shared. The original [MJDP09YL Mesh bulb PR](https://github.com/bramstroker/homeassistant-powercalc/pull/2563) records manufacturer `Xiaomi` and exact model `Mesh: MJDP09YL, yeelink.light.mbulb3`; the [YLDP13YL PR](https://github.com/bramstroker/homeassistant-powercalc/pull/756) records `color4`, which is not unique in the current device table; and the [YLXD024 PR](https://github.com/bramstroker/homeassistant-powercalc/pull/4133) confirms unique discovery alias `ceild`.
- [Ynoa's Smart ZigBee LED Lamp guide](https://www.ledlampenkopen.nu/mpattachments/file/download/id/42/product_id/1771/) identifies `LA-GU10-RGBW` as the article number and `ZigBee RGBW M350` as its app reference. [Zigbee2MQTT's Ynoa device definition](https://github.com/Koenkk/zigbee-herdsman-converters/blob/master/src/devices/ynoa.ts) maps unique raw Zigbee model `ZBT-RGBWLight-M0000` to `LA-GU10-RGBW`; [the original profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/1534) documents the corrected four-lamp measurement without a dummy load.
- [YouLess' official LS120 downloads](https://www.youless.nl/downloads.html) identify `LS120` as the energy-monitor model. [Home Assistant's YouLess documentation](https://www.home-assistant.io/integrations/youless/) distinguishes `LS110` from `LS120` and lists firmware variants separately, while [the entity implementation](https://github.com/home-assistant/core/blob/dev/homeassistant/components/youless/entity.py) exposes the device-reported model with manufacturer `YouLess`.
- [The official Zemismart Moon Lamp page](https://www.zemismart.com/products/christmas-gift-led-remote-control-light-compatible-with-alexa-google-home-3d-printing-children-bedroom-colorful-moon-lamp-app-121) identifies the product but does not publish a reliable model number. [ESPHome Devices' Moonlamp definition](https://devices.esphome.io/devices/zemismart-moonlamp/) documents the configurable node name, RGB plus cold/warm-white channels, and potentially higher consumption under ESPHome; [the original profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/1313) records that the measured lamp used ESPHome.
- [Aqara's security and compliance tables](https://www.aqara.com/en/security-certifications/) map official product names to exact model codes and regional variants.
- [Aqara's supported-device list](https://opendoc.aqara.com/en/docs/SDKDevelopment/IOSDevelopmentGuide/Equipmentcontrol/DeviceControlSDKSupportedDeviceList.html) maps product variants to their `lumi.*` and Matter discovery identifiers.
- [Arlec's ALD295HA product page](https://www.arlec.com.au/products/arlec-white-740lm-grid-connect-smart-led-downlight-with-flushed-lens) identifies the individual downlight model.
- [Arlec's ALD295P5HA product page](https://www.arlec.com.au/products/arlec-white-740lm-grid-connect-smart-led-downlight-with-flushed-lens-5-pack) shows the corresponding five-pack SKU.
- Arlec's current [R80 bulb](https://www.arlec.com.au/products/arlec-white-grid-connect-smart-r80-led-806lm-9w-es-cct-globe) and [12W B22 bulb](https://www.arlec.com.au/products/arlec-white-grid-connect-smart-b22-led-1050lm-12w-cct-colour-mode-globe) pages identify replacement models `GLD315HA` and `GLD310HA`; retailer evidence shows the old Bunnings item numbers persisted, so those numbers cannot establish revision equivalence.
- [Athom's legacy bulb product page](https://de.athom.tech/blank-1/color-bulb) retains the `LB01-7W-E27` article number, but its current 12W content illustrates why stale pages require corroboration.
- [Athom's official bulb configuration](https://github.com/athom-tech/athom-configs/blob/main/athom-rgbww-light.yaml) and [plug configuration](https://github.com/athom-tech/athom-configs/blob/main/athom-smart-plug-v2.yaml) define the current ESPHome project manufacturer and model values.
- [ESPHome Devices' Athom 7W bulb page](https://devices.esphome.io/devices/athom-e27-7w-bulb/) documents the legacy `athom.rgbww-light` project and measured product specifications.
- [ESPHome Devices' Athom AU V2 plug page](https://devices.esphome.io/devices/athom-smart-plug-pg05v2-au10a/) maps `PG05V2-AU10A` to the historical `athom.smart-plug-v2` project.
- [Home Assistant's ESPHome manager](https://github.com/home-assistant/core/blob/dev/homeassistant/components/esphome/manager.py) splits ESPHome project names into manufacturer and model discovery values.
- [Home Assistant's Bang & Olufsen config flow](https://github.com/home-assistant/core/blob/dev/homeassistant/components/bang_olufsen/config_flow.py) uses the discovered or selected product model for current Mozart devices.
- [Home Assistant's Bang & Olufsen setup](https://github.com/home-assistant/core/blob/dev/homeassistant/components/bang_olufsen/__init__.py) registers the manufacturer as `Bang & Olufsen` and the selected model as device information.
- [The original Beoplay M5 profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/3969) records `2714 CA16` as its Home Assistant Device Info model.
- [The original Beosound Edge profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/3967) records `6661 S53` as its Home Assistant Device Info model.
- [Bosch's software and security update table](https://www.bosch-smarthome.com/nl/nl/software-securityupdates/) identifies `BSHC-1` as the first-generation controller's type designation.
- [Bosch's Smart Plug Compact manual](https://www.bosch-smarthome.com/rom/plug-manual) documents `BSP-EZ` and `BSP-FZ` as device type designations with matching electrical specifications.
- [Bose's SoundTouch 10 owner guide](https://assets.bose.com/content/dam/Bose_DAM/Web/consumer_electronics/global/products/speakers/soundtouch_10_wireless_music_system/pdf/785169_og_soundtouch-10-wireless-system_en.pdf) documents type designation `416776`.
- [Bose's SoundTouch 300 owner guide](https://assets.bose.com/content/dam/Bose_DAM/Web/consumer_electronics/global/products/speakers/st_300_product_page/pdf/773965_og_soundtouch-300-soundbar_en.pdf) documents type designation `421650`.
- [Home Assistant's SoundTouch integration](https://github.com/home-assistant/core/blob/dev/homeassistant/components/soundtouch/media_player.py) uses `Bose Corporation` and the device API type for discovery.
- [Cree's Connected bulb FAQ](https://cms.creelighting.com/app/uploads/dlm_uploads/2021/09/faq-connected-oct26_1.pdf) documents the 60W replacement A19 Zigbee bulb and its `4Flow` filament design.
- [Cree's Connected Max support page](https://www.creelighting.com/resources/for-consumers/connected-max-set-up-and-support/) provides official documentation for Connected Max smart bulbs.
- [Dreame's declarations of conformity](https://global.dreametech.com/pages/declaration-of-conformity) map product names to radio-equipment and type codes.
- [The Dreame Vacuum integration entity implementation](https://github.com/Tasshack/dreame-vacuum/blob/master/custom_components/dreame_vacuum/entity.py) exposes the device-reported manufacturer and model to Home Assistant.
- [Dreo's product security policy](https://www.dreo.com/pages/product-security-policy) lists supported products by exact model code.
- [The official Dreo HA integration device mapping](https://github.com/dreo-team/hass-dreoverse/blob/master/README.md) maps exact model codes to current product names and supported speeds.
- [The official Dreo HA integration entity implementation](https://github.com/dreo-team/hass-dreoverse/blob/master/custom_components/dreo/entity.py) exposes the cloud model separately from the device name.
- [EGLO technical data sheets for 900053](https://tools.eglo.com/tds/eng-GB/900053), [900084](https://tools.eglo.com/tds/eng-GB/900084), and [98847](https://tools.eglo.com/tds/eng-GB/98847) identify the material number, product family, fixture type, dimensions, and rated maximum power.
- [The original EGLO 900053](https://github.com/bramstroker/homeassistant-powercalc/pull/4075) and [900084](https://github.com/bramstroker/homeassistant-powercalc/pull/4078) profile discussions document that distinct physical lights can share the AwoX `33955` discovery value.
- [Elgato's Key Light identification guide](https://help.elgato.com/hc/en-us/articles/20935383021965-Elgato-Key-Light-How-to-Identify-if-You-Have-a-Key-Light-or-Key-Light-MK-2) distinguishes original model `20GAK9901` from MK.2 model `20GAK9902`; its [model-number guide](https://help.elgato.com/hc/en-us/articles/360052737551-Finding-Serial-and-Model-Numbers-of-Elgato-Devices) shows where to verify the hardware code.
- [Corsair's Ring Light declaration](https://cwsmgmt.corsair.com/documents/CE-000431AB%20Elgato%20Ring%20Light.pdf) and [Light Strip declaration](https://cwsmgmt.corsair.com/documents/UKCA-000023AA%20Elgato%20Light%20Strip.pdf) establish the regulatory models `20LAC9901` and `20LAA9901`.
- The original [Elgato Key Light](https://github.com/bramstroker/homeassistant-powercalc/pull/967), [Key Light Air](https://github.com/bramstroker/homeassistant-powercalc/pull/860), [Ring Light](https://github.com/bramstroker/homeassistant-powercalc/pull/1499), [Light Strip](https://github.com/bramstroker/homeassistant-powercalc/pull/3197), and [Light Strip Pro](https://github.com/bramstroker/homeassistant-powercalc/pull/3200) profile PRs preserve the exact Home Assistant product-string aliases and profile provenance.
- The original Elgin [48BLEDWIFI00](https://github.com/bramstroker/homeassistant-powercalc/pull/2290), [48BLED15WIFI](https://github.com/bramstroker/homeassistant-powercalc/pull/2291), and [48LSB30WIFI0](https://github.com/bramstroker/homeassistant-powercalc/pull/3436) profile PRs contain product-label wattage/model evidence and, where visible, exact Tuya-reported model values. Elgin's [current product page](https://www.elgin.com.br/lampada-led-smart-30w-compativel-com-alexa-e-google-home-bivolt/p) still identifies the 30W product assets with EAN `7908412514418`, while its [smart-home catalog](https://eadconstrusete.com.br/wp-content/uploads/2024/11/Catalogo-Casa-Inteligente.pdf) assigns EAN `7897013585661` to the same complete `48LSB30WIFI0` code and specifications.
- [Innr product pages](https://innr.com/collections/frontpage) list official product names and type numbers.
- [Innr declarations of conformity](https://innr.com/pages/declarations-of-conformity) corroborate exact type codes and regional variants.
- [Innr's RF 263-2 product presentation](https://www.lightshopmalta.com/viewer/52713/download.pdf) specifies 4.2W and 350 lm; [Gamma's exact two-pack EAN record](https://www.gamma.nl/assortiment/innr-rf-263-2-slimme-e27-filament-lamp-2-pack/p/B135793) corroborates 350 lm for `8718781552022`. A later [RF 263-2 /05 reseller feed](https://www.quzo.co.uk/smart-fil-bulb-e27-z30-2pack-rf263205-pid354645.html) illustrates why internally conflicting generated specifications must not override the coherent product evidence.
- Gamma's historical records tie `RB 148 T` [single-pack EAN `8718781551643`](https://www.gamma.nl/assortiment/innr-led-lamp-e14-instelbaar-wit/p/B605766?sg=a) and [two-pack EAN `8718781551810`](https://www.gamma.nl/assortiment/innr-led-lampen-e14-2-pack-verstelbaar-wit/p/B605767) to 5.3W/470 lm. [ROBBshop's historical `RB 185 C` record](https://www.robbshop.nl/inn-e27-lamp-kleur-rb185c) ties EAN `8718781551377` to 9.5W/806 lm. The original [RB 148 T PR](https://github.com/bramstroker/homeassistant-powercalc/pull/1459) and [RB 185 C PR](https://github.com/bramstroker/homeassistant-powercalc/pull/1468) show both profiles were measured with the same Nous A1T setup and measurement-tool version within four days.
- Historical `RB 175 W` listings show why a shared type code and EAN do not prove one electrical revision: [Gamma's older exact-EAN record](https://www.gamma.nl/assortiment/innr-led-lamp-e27-dimbaar-warm-wit/p/B535024) specifies 10.5W, while [ROBBshop's exact-EAN record](https://www.robbshop.nl/inn-e27-lamp-rb175w) and [ASIN-linked historical reviews](https://angola.desertcart.com/products/55010861-innr-e-27-smart-led-bulb-warm-dimming-2200-k-2700-k-compatible-with-hue-echo-plus-amp-alexa-rb-175-w) specify 9W. The measured profile's 9.17W peak identifies it as the 9W revision.
- [Malmbergs' 9970020 product page](https://www.malmbergs.com/Products/9970020) identifies the article as the 36W Sirius III WiFi Tune LED panel.
- [The original Malmbergs profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/934) links the measured profile directly to article `9970020` and documents brightness and colour-temperature coverage.
- [Mammotion's YUKA mini product page](https://eu.mammotion.com/products/yuka-mini-robot-lawn-mower) identifies `YUKA mini 600` as the product model and describes it as a perimeter-wire-free robot lawn mower.
- [The original Mammotion profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/3523) records the identical Home Assistant model and model ID and documents the multi-cycle charging and operating-state measurements.
- [MeLiTec's official website](https://www.melitec.de/) uses the `MeLiTec` company and brand capitalization.
- [The original LightZone DP15 profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/805) contains the device type-plate evidence and establishes `LightZone` as the ESPHome discovery manufacturer alias; [the replacement measurement PR](https://github.com/bramstroker/homeassistant-powercalc/pull/1188) documents the corrected colour-temperature LUT.
- [Mercator's Walter Zigbee downlight page](https://mercator.com.au/products/walter-9w-90mm-ikuu-smart-zigbee-rgb-and-cct-led-downlight), [Ikuü's measurement-era 92 mm page](https://www.ikuu.com.au/product/92mm-walter-downlight-rgb-cct-2/), and the [official data sheet](https://www.ikuu.com.au/downloadable-assets/product-specifications/MIDS000010Z%20Data%20Sheet.pdf) identify SKU `SMD4109W-RGB-ZB`, its product family and specifications.
- [The original Mercator Ikuü profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/2618) records ZHA model `TS0505B` and manufacturer `_TZ3210_xr5m6kfg`; the [current Zigbee2MQTT converter](https://github.com/Koenkk/zigbee-herdsman-converters/blob/master/src/devices/mercator.ts) maps fingerprint `TS0505B` with `_TZ3000_xr5m6kfg` to the same SKU.
- [Meross' official MSL120 product page](https://www.meross.com/en-gc/smart-light/light-bulb-that-changes-color/28) identifies the model as `MSL120`, provides MSL120 manuals, and documents the regional bases and nominal power specifications.
- [The original Meross profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/1127) identifies the measured AU product as MSL120 and records `msl120d 2.0.0` as the model exposed to Home Assistant.
- [MOES' official Matter GU10 product page](https://moeshouse.com/products/tuya-gu10-matter-wifi-smart-led-bulb-with-voice-control-dimmable-5w-light-2700-6500k-rgb-colors) identifies single-lamp SKU `MWB-TD5-RCW-GU10-MS`; [PR #3024](https://github.com/bramstroker/homeassistant-powercalc/pull/3024) records the GU10 hardware and Matter model ID `1412`.
- [MOES' official Zigbee downlight page](https://moeshouse.com/products/zigbee-smart-light-zb-lzd7-rcw-en) maps the 10W variant to SKU `ZB-LZD10-RCW-EN`; [PR #3996](https://github.com/bramstroker/homeassistant-powercalc/pull/3996) and [replacement measurement PR #4020](https://github.com/bramstroker/homeassistant-powercalc/pull/4020) record Home Assistant model `ZB-LZD10-RCW` and the measured 10W profile.
- [Müller Licht's official product-information page](https://www.mueller-licht.de/produktinformationen/artikel/?q=404094) identifies `404094` as the article number of the 30 cm tint Amela white+ambiance ceiling light; [PR #3010](https://github.com/bramstroker/homeassistant-powercalc/pull/3010) records `45727` as the separate Home Assistant model ID reported for that measured light.
- [The original tint 404005 profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/2651) confirms the printed article number and documents that `ZBT-ExtendedColor` identifies a broader family of RGB+CCT lamps rather than one physical model.
- [Nanoleaf's official product-support table](https://support.nanoleaf.me/hc/en-us/articles/34733320939028-FAQ-Warranty-Nanoleaf-Product-Support-Security) identifies `NL67*` as the Matter A19/A60/E26/E27 bulb family and confirms that Nanoleaf model variations can start with `NF0`; [Nanoleaf's product page](https://nanoleaf.me/en-ca/products/nanoleaf-led-smart-bulbs) identifies `NF080B03-1A19E` as the single A19/E26 bulb SKU.
- [The EU product-information sheet](https://eprel.ec.europa.eu/fiches/lightsources/Fiche_1343226_EN.pdf) identifies `NL67E200` as the 9W E27 model; [PR #3762](https://github.com/bramstroker/homeassistant-powercalc/pull/3762) records Home Assistant's separate `Essentials A19/A60` model and Matter product ID `67` for the measured bulb.
- [Netatmo's official NHC declaration](https://www.netatmo.com/document/b8b2bb88-25c3-4ec9-8b3a-5b0844c7e22f.pdf) identifies the product model as `NHC01`; [PR #3949](https://github.com/bramstroker/homeassistant-powercalc/pull/3949) records `Smart Indoor Air Quality Monitor` as the exact Home Assistant model for the measured device.
- [NETGEAR's GS110MX/GS110EMX datasheet](https://www.netgear.com/images/datasheet/switches/webmanagedswitches/GS110EMX_GS110MX.pdf) identifies `GS110MX` as the unmanaged model with eight Gigabit ports and two 10-Gigabit/Multi-Gigabit uplinks; [PR #4471](https://github.com/bramstroker/homeassistant-powercalc/pull/4471) documents manual-only use and the measured 8W operating value.
- [The NAS-WR01Z instruction manual](https://fccid.io/Z52NAS-WR01Z/User-Manual/User-manual-2948691.pdf) prints model `NAS-WR01Z` and identifies the product as a Z-Wave power plug from Shenzhen NEO Electronics; [the original measurement discussion](https://github.com/bramstroker/homeassistant-powercalc/discussions/207#discussioncomment-3417324) records the relay-off and relay-on self-usage; [PR #1146](https://github.com/bramstroker/homeassistant-powercalc/pull/1146) added the profile and exact Home Assistant manufacturer mapping.
- [The Z-Wave Alliance catalog](https://products.z-wavealliance.org/z-wave-product/z-wave-plus-micro-smart-plug-on-off/) identifies `MSP-3-1-X1` as the certified NodOn Micro Smart Plug family model; [NodOn's declaration](https://nodon.fr/wp-content/uploads/2018/07/EU-Declaration-of-Conformity-MSP-3-1.pdf) lists the distinct regional models; [the profile history](https://github.com/bramstroker/homeassistant-powercalc/commit/450bd259faa62587a7e1beb5c10fe502d050502a) records `MSP-3-1-X1` and `ID-RF` as the exact discovery mappings.
- NOUS's official [A1T product page](https://nous.technology/product/a1t.html) and [A1Z product page](https://nous.technology/product/a1z-1.html) establish the product codes and metering features; [PR #1820](https://github.com/bramstroker/homeassistant-powercalc/pull/1820) records `NOUS A1T` as the Tasmota-reported model, while [PR #2382](https://github.com/bramstroker/homeassistant-powercalc/pull/2382) records `TS011F` with `_TZ3000_2putqrmw` for A1Z discovery.
- [Nuki's official Bridge shop link](https://nuki.io/en-gb/products/bridge?chooser=true&sku=220694) identifies the product and embeds SKU `220694`; [PR #4132](https://github.com/bramstroker/homeassistant-powercalc/pull/4132) records `Hardware Bridge`, `Nuki Home Solutions GmbH`, and the separate device serial number in Home Assistant Device Info.
- [The original Oz Smart Things profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/1351) records `DL41-03-10-R-ZB` from the packaging and identifies the measured product as a 10W RGBW Zigbee downlight; the [current product page](https://www.ozsmartthings.com.au/products/oz-smart-rgbw-zigbee-downlight) now identifies its offering as V2 with SKU `OZSMART-ZIG-DL`, which is not sufficient evidence of electrical equivalence.
- [The original 3A Smart Home 12W downlight PR](https://github.com/bramstroker/homeassistant-powercalc/pull/1089) records the `LXT56-LS27LX1.7` Zigbee discovery value; the [contributor's follow-up](https://github.com/bramstroker/homeassistant-powercalc/pull/1089#issuecomment-5460397336) confirms `HGZB-DLC4-N12B` on the supplied driver and the measured product's 12 W, 720–800 lm, and 90 mm specifications.
- [Apple's NCC product table](https://images.apple.com/tw/nccid/) maps HomePod generations to their A-number hardware models.
- [Home Assistant's Apple TV integration](https://github.com/home-assistant/core/blob/dev/homeassistant/components/apple_tv/__init__.py) uses pyatv's normalized model name unless the model is unknown.
- [pyatv's model conversion](https://github.com/postlund/pyatv/blob/master/pyatv/convert.py) defines the exact HomePod model strings exposed by Home Assistant.
- [Home Assistant's Sonos entity implementation](https://github.com/home-assistant/core/blob/dev/homeassistant/components/sonos/entity.py) maps the speaker product name to `model` and `modelNumber` to `model_id`.
- [Google's ecodesign test summaries](https://support.google.com/product-documentation/answer/9851803) map Google Home and Nest product names to official hardware model numbers and networked-standby behavior.
- [Home Assistant's Cast entity implementation](https://github.com/home-assistant/core/blob/dev/homeassistant/components/cast/media_player.py) maps the Cast `model_name` and manufacturer directly to Device Info without setting `model_id`.
- [The ESPHome Gosund SL2 device definition](https://devices.esphome.io/devices/Gosund-SL2/) and [Tasmota template](https://templates.blakadder.com/gosund_SL2.html) corroborate the `SL2` model spelling and RGB controller layout.
- [The original Gosund SL2 profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/3815) documents the complete measurement and the meter's 15-second update interval.
- [Govee's supported-product model catalog](https://developer.govee.com/docs/support-product-model) confirms established `H...` codes used by its device API.
- [Govee's H7020/H7021 product page](https://us.govee.com/products/refurbished-govee-rgbic-warm-white-wi-fi-bluetooth-smart-outdoor-string-lights) distinguishes the 48ft and 96ft variants of the same product family.
- [Govee's H61F5 EPREL sheet](https://eprel.ec.europa.eu/fiches/lightsources/Fiche_2069578_DE.pdf) specifies one 500mm internal section; the [official product page](https://uk.govee.com/products/govee-strip-light-2-pro) establishes a 5m complete strip, and [PR #3853](https://github.com/bramstroker/homeassistant-powercalc/pull/3853) provides the corroborating complete-fixture LUT.
- [The original Govee H600D profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/3784) records Matter manufacturer `Shenzhen Qianyan Technology` and the exact Home Assistant Device Info.
- [The original Govee H6076 profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/1955) documents the integration's invalid but real `brightness`, `color_temp`, and `hs` color-mode combination.
- [The GreenWave PowerNodes user guide](https://www.opensmarthouse.org/zwavedatabase/264/reference/z-wave-greenwave-powernode-manual.pdf) distinguishes one-port and multi-port hardware and documents PowerNode self-metering behavior.
- [openHAB's GWPN1 device definition](https://github.com/openhab/org.openhab.binding.zwave/blob/main/doc/greenwave/gwpn1_3_0.md) identifies `GWPN1` as the one-port discovery family compatible with both `NS210` and `NS310`.
- [The original GreenWave smart-plug profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/1006) records the manually confirmed off-state and relay-on self-usage values.
- [The GreenWave manufacturer-alias PR](https://github.com/bramstroker/homeassistant-powercalc/pull/1099) establishes `GreenWave Reality Inc.` as the Home Assistant discovery manufacturer.
- [The official Hampton Bay HB-10521-HS product page](https://www.homedepot.com/p/318145796) identifies the model, 12-light count, 24ft lighted length, RGBW capability and Hubspace platform.
- [The HB-10521-HS use-and-care guide](https://images.homedepot.ca/pdf/p_1001725401_alt_HB-10521-HS_RGBw_CSL_Use-and-Care-Guide.pdf) corroborates the model and official `Cafe String Light` product description.
- [The original Hampton Bay profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/3030) records exact Home Assistant Device Info, measurement settings and the unsupported-effects limitation.
- [The official Citation One MKII product page](https://www.harmankardon.com/HKCITAONEMKIIBLKAS.html) distinguishes the MKII generation and shows that the item number includes region and finish information.
- [The Citation MultiBeam 700 service manual](https://support.harmankardon.com/on/demandware.static/-/Sites-masterCatalog_Harman/default/dwc870da7d/pdfs/Harman%20Kardon%20Citation%20Multibeam%20700%20-%20Service%20Manual%20V1.01.pdf) specifies 2.0W network standby after 18 minutes.
- [The original Citation MultiBeam 700 profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/1047) documents the volume calibration and separate idle and delayed network-standby measurements.
- [The original Citation One MKII profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/1908) records that Cast reports the shared `HK Citation One` model and the intentional decision to retain it as an alias.
- [Hornbach's SMART HOME compatibility list](https://media.hornbach.at/cmsm/de/kompatibilitaetsliste-shbh.pdf) maps article `10454469` to the FLAIR Viyu E27 CCT bulb.
- [The official Hornbach product instructions](https://media.hornbach.de/hb/safetywarning/as.125568539.pdf) print article `10454469` and specify 8.5W, 806 lm, 2200–5000 K and at most 0.5W standby.
- [The official Hornbach product page](https://www.hornbach.de/p/flair-viyu-led-lampe-dimmbar-e27-8-5w60w-806-lm-2200-5000-k-kompatibel-mit-smart-home-by-hornbach/10454469/) identifies the exact product and article number.
- [The original Hornbach profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/4541) records the exact Zigbee manufacturer and model, two-lamp measurement setup and per-lamp results.
- [Home Assistant's IPP entity implementation](https://github.com/home-assistant/core/blob/dev/homeassistant/components/ipp/entity.py) maps IPP manufacturer, model and serial number to separate Device Info fields and does not set `model_id`.
- [HP's Color LaserJet Pro MFP 3301-3304/3388 user guide](https://kaas.hpcloud.hp.com/pdf-public/pdf_7579882_en-US-1.pdf) lists the product variants and distinguishes product model numbers from regulatory model numbers.
- [The original HP MFP 3301 profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/4052) records the exact IPP model, measured idle power and the polling limitation for short print activity.
- [Inovelli's device-manual index](https://help.inovelli.com/en/articles/8426779-device-manuals) maps the Blue Series Dimmer and mmWave Presence Dimmer to SKUs `VZM31-SN` and `VZM32-SN`.
- [Zigbee2MQTT's Inovelli definitions](https://github.com/Koenkk/zigbee-herdsman-converters/blob/master/src/lib/inovelli.ts) corroborate the exact Zigbee model strings.
- [Intelbras' EWS 410 datasheet](https://backend.intelbras.com/sites/default/files/2023-07/EWS%20410%20-%20Datasheet%20v2_0.pdf) prints the spaced model code and documents the 10W RGB+CCT E27 specifications.
- [The original Intelbras profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/3435) contains the matching product-label photo and generic Tuya Local Device Info.
- [JBL's Link Music declaration of conformity](https://be.jbl.com/on/demandware.static/-/Sites-masterCatalog_Harman/default/dw1eb58b8e/pdfs/JBL_LINK_MUSIC-EU-UK-DOC.pdf) identifies `LINK MUSIC` as the product number and `Wireless Speaker` as the product type.
- [JBL's Link Music product page](https://www.jbl.com/JBLLINKMUSICGYAM-Z.html) distinguishes the regional item number from the `JBL Link Music` product name and describes it as a Wi-Fi speaker.
- [The original JBL Link Music profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/3087) records the exact Cast Device Info values and measurement context.
- [Kauf's BLF10 product page](https://kaufha.com/blf10/) identifies the bulb family and documents its ESPHome-based Home Assistant discovery.
- [Kauf's official RGBWW bulb firmware repository](https://github.com/KaufHA/kauf-rgbww-bulbs) documents the shared RGBWW firmware and explains that `(4m)` on the housing denotes flash size.
- [The original Kauf BLF10 profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/2472) records the printed model and confirms that Kauf bulbs of different sizes and wattages report the same generic `RGBWW` model in Home Assistant.
- [Kogan's KASB10WE27HA/KASB10WB22HA manual](https://assets.kogan.com/files/usermanuals/KASB10WE27HA_KASB10WB22HA-A.pdf) identifies the exact E27 and B22 product codes and documents adjustable color temperature.
- [Kogan's bulb reset guide](https://help.kogan.com/articles/Knowledge/FactoryResettingYourKoganSmarterHomeSmartLightsBulbs) lists `KASB10WB22HA` and its sibling variants separately.
- [The original Kogan profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/3339) records the measured `KASB10WB22HA` product and use of Tuya Local without supplying an exact discovery model.
- [Twinkly's Strings product guide](https://help.twinkly.com/hc/en-gb/articles/18277826963741-what-s-twinkly-strings) maps the 600-LED Multicolor and Multicolor + White variants to `TWS600STP` and `TWS600SPP` and documents their 36W supplies.
- [Twinkly's professional brochure](https://blog.festive-lights.com/blog/wp-content/uploads/2021/01/Twinkly-Professional-Brochure-2021.pdf) maps regional Strings SKUs to their barcodes and power supplies.
- [The original Ledworks profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/2724) records exact Home Assistant model `TWS600STP`, manufacturer `LEDWORKS`, and the extended measurement showing approximately constant on-state draw across effects and brightness settings.
- [Lenovo's Smart Clock support page](https://smartsupport.lenovo.com/us/en/products/smart/smart-home/smart-clock) identifies the first-generation Smart Clock as `CD-24501F`; Lenovo's [open-source portal](https://support.lenovo.com/cc/nb/solutions/HT511330) distinguishes it from Smart Clock 2 model `CD-24502F`.
- [The original smart-speaker profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/1047) records the Smart Clock measurement method and calibration, while [PR #1303](https://github.com/bramstroker/homeassistant-powercalc/pull/1303) added the exact `Lenovo Smart Clock` discovery alias.
- [ADEO's Lexman product-information sheet](https://media.adeo.com/media/1735618/media.pdf) identifies `IG-CDZOTAAG014RA-MAN` as the supplier model, 14W connected RGBW E27 light source, while [Leroy Merlin's product page](https://www.leroymerlin.fr/produits/ampoule-led-connectee-globe-95mm-e27-100w-variation-blanc-couleur-lexman-enki-84372273.html) uses `84372273` as its retail reference.
- [The original Lexman profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/628) records the exact `IG-CDZOTAAG014RA-MAN` directory and LUT measurement.
- [The FCC filing for Lumiman LM530](https://fccid.io/2A3YT-LM530) confirms the `LM530` product code and 2.4 GHz LED-bulb hardware family.
- [The original Lumiman LM530 profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/2066) identifies the measured US A19 product and records that its generic LocalTuya Device Info is suitable only for manual selection.
- [The Lumiman LM530 E26 device template](https://templates.blakadder.com/lumiman_LM530-E26.html) corroborates the US E26 variant's 7.5W, 800 lm, and RGBW specifications.
- [Lutron's Lamp Dimming Smart Plug product page](https://www.casetawireless.com/us/en/products/smart-plugs/lamp-dimming-smart-plug) maps the US white variant to `PD-3PCL-WH` and identifies it as a plug-in lamp dimmer.
- [Home Assistant's Lutron Caséta implementation](https://github.com/home-assistant/core/blob/dev/homeassistant/components/lutron_caseta/__init__.py) constructs Device Info model text from the device model and parenthesized type.
- [The original Lutron profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/3358) records `PD-3PCL-WH (PlugInDimmer)`, the exact manufacturer value, and the low-resolution standby sample.
- [The Philips 4000i Series product page](https://www.philips.com/c-p/AC4236_10R1/4000i-series-air-purifier) establishes the official English product-family wording and identifies a separate regional model variant.
- [The original Philips AC4236/14 profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/4531) records the exact Home Assistant model, integration, measurements and regional device variant.
- [The `philips_airpurifier_coap` entity implementation](https://github.com/kongo09/philips-airpurifier-coap/blob/master/custom_components/philips_airpurifier_coap/philips.py) maps the device-status model to Home Assistant Device Info.
- [Qubino's Smart Plug 16A technical specifications](https://qubino.com/products/smart-plug-16a/smart-plug-16a-tech/) identify `ZMNHYD1` and distinguish the regional frequency-specific ordering codes.
- [The Z-Wave JS Smart Plug 16A configuration](https://github.com/zwave-js/zwave-js/blob/master/packages/config/config/devices/0x0159/smart_plug_16a.json) maps fingerprint `0x0002:0x0054` to the shared `Smart Plug 16A` label.
- [The original smart-plug measurement discussion](https://github.com/bramstroker/homeassistant-powercalc/discussions/207) records 0.3 W relay-off and 0.5 W relay-on consumption for the Qubino Smart Plug 16A.
- [The Reolink E1 Pro quick-start guide](https://m.reolink.com/files/docs/specs/QSG/e1-pro/EN-E1-Pro.pdf) identifies `E330` as the item number for the E1 Pro.
- [The original Reolink E1 Pro profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/3851) records exact Home Assistant Device Info model `E1 Pro (E330)` and separate day/infrared measurements.
- [Reolink's RLC-810WA product page](https://reolink.com/us/product/rlc-810wa/) confirms the model, infrared and spotlight capabilities, and a maximum draw below 12 W.
- [The original RLC-810WA profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/3749) records that the 8 W value is a multi-week average with approximately 2 W fluctuation.
- [A Rituals product catalog](https://wlw-1-company-facts-media20191122174836234900000006.s3.eu-central-1.amazonaws.com/b409f722-3e98-4810-bea0-942ace4c7a1f.pdf) identifies the Perfume Genie diffuser as article `1106834`; the [former official product URL](https://www.rituals.com/eng-at/the-perfume-genie-2.0-1106834.html) also embedded that code.
- [Rituals' Perfume Genie FAQ](https://www.rituals.com/en-gb/faq/perfumegenie) distinguishes generations 1.0 and 2.0 and describes the bottom-label LOT number as support information.
- [Home Assistant's Rituals Perfume Genie entity implementation](https://github.com/home-assistant/core/blob/dev/homeassistant/components/rituals_perfume_genie/entity.py) exposes manufacturer `Rituals Cosmetics` and model `The Perfume Genie 2.0` for generation 2.0.
- [deCONZ's ROB_200-017-0 device definition](https://dresden-elektronik.github.io/deconz-rest-doc/devices/robb_smarrt/rob_200-017-0_plug/) records exact model `ROB_200-017-0`, manufacturer `ROBB smarrt`, the 3680W product class and its metering clusters.
- [Zigbee2MQTT's ROB_200-017-0 definition](https://www.zigbee2mqtt.io/devices/ROB_200-017-0.html) independently maps that exact code to the ROBB Zigbee smart plug and its metering capabilities.
- [The original ROBB profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/2443) records exact Home Assistant Device Info and the average self-consumption measurements of two plugs.
- [Xiaomi's Mi Robot Vacuum support page](https://www.mi.com/global/support/article/KA-07583/) identifies product model `SDJQR01RR`.
- [Home Assistant's Roborock Device Info implementation](https://github.com/home-assistant/core/blob/dev/homeassistant/components/roborock/models.py) exposes the Roborock internal product model as both `model` and `model_id`.
- [Python-roborock's device catalog](https://github.com/Python-roborock/python-roborock/blob/main/device_info.yaml) maps internal model `roborock.vacuum.a10` to S6 MaxV and `roborock.vacuum.a15` to S7.
- [The Roborock S5 Max product sheet](https://static.elongroup.se/Document/Article/203768/facts-sheet-dammsugare-roborock-s5e52-00.pdf) distinguishes white SKU `S5E02-00` from black SKU `S5E52-00`.
- [The Rye SLZC3705-A Tasmota template](https://templates.blakadder.com/rye_SLZC3705-A.html) identifies the 5W 450 lm E12 RGBCCT candle bulb and records exact template name `RYE Candlebra` with a manufacturer FCC declaration covering the hardware family.
- [The original Rye profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/1068) records the Tasmota-flashed measurement setup, the repeated low-brightness investigation and released manual model value `Candlebra`.
- [Tasmota's Home Assistant documentation](https://tasmota.github.io/docs/Home-Assistant/) explains that the template name becomes the module name reflected in Home Assistant and that the integration identifies the firmware manufacturer as Tasmota.
- [The original Zengge profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/1760) records Home Assistant model `Bulb RGBCW (0x35)` and hardware model `AK001-ZJ2104`; [the follow-up PR](https://github.com/bramstroker/homeassistant-powercalc/pull/1762) confirms that the complete parenthesized value is required for discovery.
- [`flux_led`'s supported-model table](https://github.com/lightinglibs/flux_led) identifies type `0x35` as the generic `Bulb RGBCW` protocol class rather than a unique physical product model.
- [Zipato's official Bulb 2 product page](https://www.zipato.com/product/zipato-bulb2-zwave) identifies the product name and wildcard SKU `rgbw2.*`; its [official Bulb 2 manual](https://www.zipato.com/wp-content/uploads/2017/04/rgbw2-Zipato-RGBW-gen2-Bulb-User-Manual-Z-Wave-v1.3.pdf) records the inconsistent regional `rgbw2` and `rgbwe2` spellings.
- [The original Zipato profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/1126) records the `RGBWE2` contribution and the contributor's manual confirmation of its unusual color-temperature power curve.
- [Sengled's Smart LED Starter Kits guide](https://www.sengled.com.au/wp-content/uploads/2017/11/Sengled-Smart-LED-Starter-Kits-User-Guide.pdf) identifies `E11-G13` as a 2700K soft-white A19 bulb rated at 9W and 800 lm.
- [Sengled's motion-sensor bulb guide](https://www.sengled.com.au/wp-content/uploads/2018/06/Sengled-Smart-LED-with-Motion-Sensor-User-Guide.pdf) identifies model `E13-N11` as the Smart LED with Motion Sensor PAR38 Bulb.
- [The Zigbee2MQTT Sengled converter](https://github.com/Koenkk/zigbee-herdsman-converters/blob/master/src/devices/sengled.ts) maps raw models `E11-N13`, `E11-N13A`, `E11-N14`, and `E11-N14A` to the grouped Element Extra Bright display model.
- [The original Element Extra Bright profile PR](https://github.com/bramstroker/homeassistant-powercalc/pull/2013) records exact Zigbee2MQTT Device Info model `E11-N13/E11-N13A/E11-N14/E11-N14A`.
- [Home Assistant's WiZ entity implementation](https://github.com/home-assistant/core/blob/dev/homeassistant/components/wiz/entity.py) derives the displayed model from the WiZ `moduleName`.
- [pywizlight's bulb library](https://github.com/sbidy/pywizlight/blob/master/pywizlight/bulblibrary.py) documents the structure and capability meaning of WiZ module names.
- [WiZ product specifications](https://www.wizconnected.com/en-nz/p/downlight-35-inch-recessed-downlight-8w/8720169072251) show the material number (12NC) separately from the EAN.

## Review evidence

In a finding, state all three values when relevant:

- canonical directory model ID;
- alternate integration-reported value to preserve in `aliases`;
- unstable or user-specific suffix to remove.

Example: use `E2499` as the directory, preserve `VARMBLIXT colour and white` as the Hue-reported alias, and remove the bridge resource suffix `Light0x07C2` from the display name.
