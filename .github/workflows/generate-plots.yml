name: Generate plots

on:
  push:
    paths:
      - 'profile_library/**/*.csv.gz'
      - 'profile_library/**/model.json'
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: profile-library-writes
  cancel-in-progress: false

jobs:
  format:
    name: Generate plots
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
          persist-credentials: true
      - uses: astral-sh/setup-uv@ae62891fec2bb8e7d6c99fc78c9fec3a63790f8d # v10.0.0
        with:
          version: '0.12.4'
          working-directory: './utils/measure'
          enable-cache: true
          cache-dependency-glob: |
            pyproject.toml
            uv.lock
      - name: Run visualization tool
        run: uv --project utils/measure run --locked --group visualize powercalc-visualize ./profile_library
      - uses: EndBug/add-and-commit@645ecc0dd0a57f4d86d26c0aa5fc42c0a856fbca # v11
        if: github.ref == 'refs/heads/master'
        with:
          message: 'chore(profile): generate plots'
          pull: '--rebase --autostash'
          push: 'origin HEAD:master'
